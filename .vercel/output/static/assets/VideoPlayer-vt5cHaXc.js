import { c as createLucideIcon, u as useTranslation, r as reactExports, ax as getDownloadLimitInfo, j as jsxRuntimeExports, e as ChevronLeft, av as Download, B as Button, ab as getDownloadRapidApiKey, v as ue, ay as recordDownloadLocal, X, t as cn, Y as useComposedRefs, $ as Primitive, am as Presence, a6 as createContextScope, a0 as composeEventHandlers, y as useCallbackRef, z as useLayoutEffect2, b as useActor, p as useQueryClient, d as useQuery, q as useMutation } from "./index-B4P1PGaK.js";
import { C as CircleCheck, u as useDirection } from "./index-KgqyCsxg.js";
import { c as clamp, a as ChevronUp, C as ChevronDown } from "./index-BivI3RN0.js";
import { S as Skeleton } from "./skeleton-BQhv6M21.js";
import { T as Textarea } from "./textarea-BcmhiIIK.js";
import { B as Badge } from "./badge-B5FJUKjx.js";
import { D as Dialog, a as DialogContent, b as DialogHeader, c as DialogTitle } from "./dialog-BTdr-nPe.js";
import { s as saveVideo, C as Captions } from "./offlineStorage-B7iGHUae.js";
import { C as CircleAlert } from "./circle-alert-umy4a3lv.js";
import { F as Film } from "./film-CmhOy8TL.js";
import { L as LoaderCircle } from "./loader-circle-CD345DHk.js";
import { W as WifiOff } from "./wifi-off-Dh_rJnac.js";
import { E as ExternalLink } from "./external-link-CIWNqrEm.js";
import { M as MessageCircle } from "./message-circle-BL4BDtUO.js";
import { B as BookmarkPlus } from "./bookmark-plus-DCBfQUwu.js";
import { U as Upload } from "./upload-DDDpJKii.js";
import { T as Trash2 } from "./trash-2-QrZqrw48.js";
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$7 = [
  ["rect", { width: "8", height: "4", x: "8", y: "2", rx: "1", ry: "1", key: "tgr4d6" }],
  ["path", { d: "M8 4H6a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2v-2", key: "4jdomd" }],
  ["path", { d: "M16 4h2a2 2 0 0 1 2 2v4", key: "3hqy98" }],
  ["path", { d: "M21 14H11", key: "1bme5i" }],
  ["path", { d: "m15 10-4 4 4 4", key: "5dvupr" }]
];
const ClipboardCopy = createLucideIcon("clipboard-copy", __iconNode$7);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$6 = [
  ["path", { d: "M9 18V5l12-2v13", key: "1jmyc2" }],
  ["circle", { cx: "6", cy: "18", r: "3", key: "fqmcym" }],
  ["circle", { cx: "18", cy: "16", r: "3", key: "1hluhg" }]
];
const Music = createLucideIcon("music", __iconNode$6);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$5 = [
  [
    "path",
    {
      d: "M21.174 6.812a1 1 0 0 0-3.986-3.987L3.842 16.174a2 2 0 0 0-.5.83l-1.321 4.352a.5.5 0 0 0 .623.622l4.353-1.32a2 2 0 0 0 .83-.497z",
      key: "1a8usu"
    }
  ],
  ["path", { d: "m15 5 4 4", key: "1mk7zo" }]
];
const Pencil = createLucideIcon("pencil", __iconNode$5);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$4 = [
  [
    "path",
    {
      d: "M14.536 21.686a.5.5 0 0 0 .937-.024l6.5-19a.496.496 0 0 0-.635-.635l-19 6.5a.5.5 0 0 0-.024.937l7.93 3.18a2 2 0 0 1 1.112 1.11z",
      key: "1ffxy3"
    }
  ],
  ["path", { d: "m21.854 2.147-10.94 10.939", key: "12cjpa" }]
];
const Send = createLucideIcon("send", __iconNode$4);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$3 = [
  ["circle", { cx: "18", cy: "5", r: "3", key: "gq8acd" }],
  ["circle", { cx: "6", cy: "12", r: "3", key: "w7nqdw" }],
  ["circle", { cx: "18", cy: "19", r: "3", key: "1xt0gg" }],
  ["line", { x1: "8.59", x2: "15.42", y1: "13.51", y2: "17.49", key: "47mynk" }],
  ["line", { x1: "15.41", x2: "8.59", y1: "6.51", y2: "10.49", key: "1n3mei" }]
];
const Share2 = createLucideIcon("share-2", __iconNode$3);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$2 = [
  ["path", { d: "M12 19h8", key: "baeox8" }],
  ["path", { d: "m4 17 6-6-6-6", key: "1yngyt" }]
];
const Terminal = createLucideIcon("terminal", __iconNode$2);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$1 = [
  ["path", { d: "M17 14V2", key: "8ymqnk" }],
  [
    "path",
    {
      d: "M9 18.12 10 14H4.17a2 2 0 0 1-1.92-2.56l2.33-8A2 2 0 0 1 6.5 2H20a2 2 0 0 1 2 2v8a2 2 0 0 1-2 2h-2.76a2 2 0 0 0-1.79 1.11L12 22a3.13 3.13 0 0 1-3-3.88Z",
      key: "m61m77"
    }
  ]
];
const ThumbsDown = createLucideIcon("thumbs-down", __iconNode$1);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode = [
  ["path", { d: "M7 10v12", key: "1qc93n" }],
  [
    "path",
    {
      d: "M15 5.88 14 10h5.83a2 2 0 0 1 1.92 2.56l-2.33 8A2 2 0 0 1 17.5 22H4a2 2 0 0 1-2-2v-8a2 2 0 0 1 2-2h2.76a2 2 0 0 0 1.79-1.11L12 2a3.13 3.13 0 0 1 3 3.88Z",
      key: "emmmcr"
    }
  ]
];
const ThumbsUp = createLucideIcon("thumbs-up", __iconNode);
const DEFAULT_RAPIDAPI_KEY = "54ca2c15e4mshe644cf482a6a4edp19b3fajsn6ab22879e0db";
let _debugLog = [];
function getDownloadDebugLog() {
  return [..._debugLog];
}
function logDownload(step, status, message, details) {
  const entry = {
    timestamp: (/* @__PURE__ */ new Date()).toISOString(),
    step,
    status,
    message,
    details
  };
  _debugLog.push(entry);
  const consoleMethod = status === "error" ? console.error : status === "warning" ? console.warn : console.log;
  consoleMethod(`[Download:${step}] ${message}`, details ?? "");
}
function maskKey(key) {
  if (!key || key.length < 12) return "***";
  return `${key.slice(0, 6)}...${key.slice(-4)}`;
}
function classifyHttpError(status, bodyPreview) {
  if (status === 401 || status === 403)
    return `API authorization failed (HTTP ${status}). The RapidAPI key may be invalid, expired, or rate-limited.`;
  if (status === 404)
    return `Video not found on this endpoint (HTTP ${status}). The provider may have removed the video or changed its API.`;
  if (status === 429)
    return `Rate limit exceeded (HTTP ${status}). Too many requests to RapidAPI. Wait a minute and try again.`;
  if (status >= 500)
    return `RapidAPI server error (HTTP ${status}). The download service is temporarily unavailable.`;
  if (bodyPreview == null ? void 0 : bodyPreview.toLowerCase().includes("html"))
    return `API returned an HTML page instead of JSON (HTTP ${status}). The endpoint may be down or require different parameters.`;
  return `API request failed with HTTP ${status}.`;
}
async function getVideoDownloadUrl(videoId, provider, quality, rapidApiKey) {
  var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l;
  const key = rapidApiKey || DEFAULT_RAPIDAPI_KEY;
  const lowerProvider = provider.toLowerCase();
  logDownload("getVideoDownloadUrl", "info", "Resolving download URL", {
    provider: lowerProvider,
    videoId,
    quality,
    maskedKey: maskKey(key)
  });
  if (lowerProvider === "youtube") {
    console.log("YOUTUBE RAPIDAPI CALLED", videoId);
    try {
      const ctrl = new AbortController();
      const timer = setTimeout(() => ctrl.abort(), 15e3);
      const res = await fetch(
        `https://youtube-media-downloader.p.rapidapi.com/v2/video/details?videoId=${videoId}`,
        {
          method: "GET",
          headers: {
            "X-RapidAPI-Key": key,
            "X-RapidAPI-Host": "youtube-media-downloader.p.rapidapi.com"
          },
          signal: ctrl.signal
        }
      );
      clearTimeout(timer);
      const bodyText = await res.text();
      logDownload(
        "getVideoDownloadUrl",
        res.ok ? "success" : "error",
        `YouTube API HTTP ${res.status}`,
        {
          bodyPreview: bodyText.slice(0, 500)
        }
      );
      if (!res.ok) {
        return null;
      }
      const data = JSON.parse(bodyText);
      console.log(
        "VIDEO QUALITIES:",
        (_b = (_a = data.videos) == null ? void 0 : _a.items) == null ? void 0 : _b.map((v) => ({
          quality: v.quality,
          width: v.width,
          height: v.height,
          url: v.url,
          downloadUrl: v.downloadUrl
        }))
      );
      console.log("AUDIOS:", data.audios);
      console.log("FIRST VIDEO:", (_d = (_c = data.videos) == null ? void 0 : _c.items) == null ? void 0 : _d[0]);
      console.log("FIRST AUDIO:", (_f = (_e = data.audios) == null ? void 0 : _e.items) == null ? void 0 : _f[0]);
      console.log("FORMATS:", data.formats);
      console.log("MUXED:", data.muxed);
      console.log("FIRST VIDEO:", (_h = (_g = data.videos) == null ? void 0 : _g.items) == null ? void 0 : _h[0]);
      console.log(
        "VIDEO QUALITIES:",
        (_j = (_i = data.videos) == null ? void 0 : _i.items) == null ? void 0 : _j.map((v) => ({
          quality: v.quality,
          width: v.width,
          height: v.height,
          url: v.url,
          downloadUrl: v.downloadUrl
        }))
      );
      console.log(
        "YT API RESPONSE FULL:",
        JSON.stringify(data, null, 2)
      );
      console.log("YT KEYS:", Object.keys(data));
      const videos = ((_k = data == null ? void 0 : data.videos) == null ? void 0 : _k.items) ?? [];
      if (!videos.length) {
        console.error("NO VIDEOS FOUND");
        return null;
      }
      const wantedHeight = Number(quality);
      const selected = videos.find(
        (v) => Number(v.height) === wantedHeight && v.hasAudio === true
      ) || videos.find((v) => v.hasAudio === true) || videos[0];
      console.log("SELECTED FORMAT:", selected);
      console.log(
        "RETURNING URL:",
        (_l = selected == null ? void 0 : selected.url) == null ? void 0 : _l.substring(0, 120)
      );
      return (selected == null ? void 0 : selected.url) ?? null;
    } catch (err) {
      const msg = err instanceof Error ? err.message : String(err);
      logDownload(
        "getVideoDownloadUrl",
        "error",
        `YouTube error: ${msg}`
      );
      return null;
    }
  }
  if (lowerProvider === "tiktok") {
    logDownload(
      "getVideoDownloadUrl",
      "warning",
      "TikTok download is blocked — stream only",
      { provider: lowerProvider }
    );
    return null;
  }
  if (lowerProvider === "vimeo") {
    try {
      const ctrl = new AbortController();
      const timer = setTimeout(() => ctrl.abort(), 12e3);
      const res = await fetch(`https://api.vimeo.com/videos/${videoId}`, {
        headers: {
          Authorization: `bearer ${key}`,
          Accept: "application/vnd.vimeo.*+json;version=3.4"
        },
        signal: ctrl.signal
      });
      clearTimeout(timer);
      const bodyText = await res.text();
      logDownload(
        "getVideoDownloadUrl",
        res.ok ? "success" : "error",
        `Vimeo API HTTP ${res.status}`,
        { bodyPreview: bodyText.slice(0, 300) }
      );
      if (!res.ok) return null;
      const data = JSON.parse(bodyText);
      const downloads = data.download ?? data.files ?? [];
      const qual = quality.replace("p", "");
      const match = downloads.find(
        (d) => d.quality.includes(qual) || d.quality.includes(quality) || qual === "720" && d.quality.includes("hd")
      );
      if (match == null ? void 0 : match.link) {
        logDownload(
          "getVideoDownloadUrl",
          "success",
          "Vimeo download URL resolved",
          { quality: match.quality }
        );
        return match.link;
      }
      const first = downloads.find((d) => d.link);
      if (first == null ? void 0 : first.link) {
        logDownload(
          "getVideoDownloadUrl",
          "success",
          "Vimeo download URL resolved (fallback)",
          { quality: first.quality }
        );
        return first.link;
      }
      logDownload(
        "getVideoDownloadUrl",
        "warning",
        "Vimeo response has no download links"
      );
      return null;
    } catch (err) {
      const msg = err instanceof Error ? err.message : String(err);
      logDownload("getVideoDownloadUrl", "error", `Vimeo error: ${msg}`);
      return null;
    }
  }
  if (lowerProvider === "dailymotion") {
    try {
      const ctrl = new AbortController();
      const timer = setTimeout(() => ctrl.abort(), 1e4);
      const res = await fetch(
        `https://api.dailymotion.com/video/${videoId}?fields=stream_h264_url,stream_h264_hd_url,url`,
        { signal: ctrl.signal }
      );
      clearTimeout(timer);
      const bodyText = await res.text();
      logDownload(
        "getVideoDownloadUrl",
        res.ok ? "success" : "error",
        `Dailymotion API HTTP ${res.status}`,
        { bodyPreview: bodyText.slice(0, 300) }
      );
      if (!res.ok) return null;
      const data = JSON.parse(bodyText);
      const url = data.stream_h264_hd_url ?? data.stream_h264_url ?? data.url ?? null;
      if (url) {
        logDownload(
          "getVideoDownloadUrl",
          "success",
          "Dailymotion stream URL resolved"
        );
      }
      return url ?? null;
    } catch (err) {
      const msg = err instanceof Error ? err.message : String(err);
      logDownload("getVideoDownloadUrl", "error", `Dailymotion error: ${msg}`);
      return null;
    }
  }
  if (lowerProvider === "archive" || lowerProvider === "internetarchive") {
    const url = `https://archive.org/download/${videoId}/${videoId}.mp4`;
    logDownload(
      "getVideoDownloadUrl",
      "success",
      "Internet Archive direct URL constructed",
      { url }
    );
    return url;
  }
  logDownload(
    "getVideoDownloadUrl",
    "warning",
    `Unknown provider: ${lowerProvider}`
  );
  return null;
}
async function downloadVideo(url, filename, rapidApiKey, onProgress) {
  if (url.includes("googlevideo.com") || url.includes("youtube.com")) {
    const res2 = await fetch(
      `https://streamverse-proxy.streamverseproxy.workers.dev/download?url=${encodeURIComponent(url)}`
    );
    const blob2 = await res2.blob();
    const blobUrl2 = URL.createObjectURL(blob2);
    triggerBlobDownload(blobUrl2, filename);
    URL.revokeObjectURL(blobUrl2);
    onProgress({
      stage: "complete",
      percent: 100
    });
    return;
  }
  const key = rapidApiKey;
  logDownload("downloadVideo", "info", "Starting blob download", {
    url: url.slice(0, 200),
    filename,
    maskedKey: maskKey(key)
  });
  onProgress({ stage: "fetching", percent: 5 });
  let res;
  try {
    res = await fetch(url, {
      mode: "cors",
      headers: {
        "X-RapidAPI-Key": key
      }
    });
  } catch (err) {
    const msg = err instanceof Error ? err.message : String(err);
    const isCors = err instanceof TypeError && (msg.includes("CORS") || msg.includes("Failed to fetch") || msg.includes("NetworkError"));
    logDownload("downloadVideo", "error", `Fetch failed: ${msg}`, {
      isCors
    });
    onProgress({
      stage: "error",
      percent: 0,
      error: {
        httpStatus: isCors ? 0 : void 0,
        providerError: msg,
        failureStage: "fetch",
        userMessage: isCors ? "تعذر التحميل بسبب قيود المتصفح (CORS) / Download blocked by browser (CORS)" : "فشل الاتصال بالخادم — تأكد من اتصال الإنترنت / Connection failed — check your internet"
      }
    });
    throw new Error(msg);
  }
  logDownload("downloadVideo", "info", `HTTP ${res.status}`, {
    statusText: res.statusText,
    contentType: res.headers.get("content-type"),
    contentLength: res.headers.get("content-length")
  });
  if (!res.ok) {
    const bodyText = await res.text().catch(() => "");
    const errMsg = classifyHttpError(res.status, bodyText);
    logDownload("downloadVideo", "error", errMsg, {
      status: res.status,
      bodyPreview: bodyText.slice(0, 500)
    });
    onProgress({
      stage: "error",
      percent: 0,
      error: {
        httpStatus: res.status,
        providerError: errMsg,
        failureStage: "api-call",
        userMessage: `فشل التحميل (${res.status}) — ${errMsg} / Download failed (${res.status}) — ${errMsg}`
      }
    });
    throw new Error(errMsg);
  }
  const contentLength = res.headers.get("content-length");
  const total = contentLength ? Number.parseInt(contentLength, 10) : 0;
  onProgress({ stage: "downloading", percent: 10 });
  if (!res.body) {
    const blob2 = await res.blob();
    const blobUrl2 = URL.createObjectURL(blob2);
    triggerBlobDownload(blobUrl2, filename);
    URL.revokeObjectURL(blobUrl2);
    onProgress({ stage: "complete", percent: 100 });
    logDownload(
      "downloadVideo",
      "success",
      "Download complete (no streaming)",
      {
        sizeBytes: blob2.size
      }
    );
    return;
  }
  const reader = res.body.getReader();
  const chunks = [];
  let loaded = 0;
  while (true) {
    const result = await reader.read();
    if (result.done) break;
    chunks.push(result.value);
    loaded += result.value.length;
    const percent = total > 0 ? Math.round(loaded / total * 100) : 0;
    onProgress({ stage: "downloading", percent: Math.max(10, percent) });
  }
  const contentType = res.headers.get("content-type") ?? "video/mp4";
  const blob = new Blob(chunks, { type: contentType });
  const blobUrl = URL.createObjectURL(blob);
  triggerBlobDownload(blobUrl, filename);
  URL.revokeObjectURL(blobUrl);
  onProgress({ stage: "complete", percent: 100 });
  logDownload("downloadVideo", "success", "Download complete", {
    sizeBytes: blob.size,
    sizeMB: (blob.size / 1024 / 1024).toFixed(2)
  });
}
function triggerBlobDownload(blobUrl, filename) {
  const a = document.createElement("a");
  a.href = blobUrl;
  a.download = filename;
  a.style.display = "none";
  document.body.appendChild(a);
  a.click();
  document.body.removeChild(a);
}
function buildWatchUrl(platform, videoId) {
  switch (platform) {
    case "vimeo":
      return `https://vimeo.com/${videoId}`;
    case "tiktok":
      return `https://www.tiktok.com/video/${videoId}`;
    default:
      return `https://www.youtube.com/watch?v=${videoId}`;
  }
}
const YT_QUALITIES = [
  { label: "Full HD", res: "1080p", vQuality: "1080", sizeFmt: "~400 MB" },
  { label: "HD", res: "720p", vQuality: "720", sizeFmt: "~200 MB" },
  { label: "SD", res: "480p", vQuality: "480", sizeFmt: "~100 MB" },
  { label: "Low", res: "360p", vQuality: "360", sizeFmt: "~60 MB" },
  {
    label: "Audio Only",
    res: "MP3",
    vQuality: "128",
    isAudio: true,
    sizeFmt: "~5 MB"
  }
];
const VIMEO_QUALITIES = [
  { label: "Best", res: "Best", vQuality: "max", sizeFmt: "~varies" },
  { label: "HD", res: "720p", vQuality: "720", sizeFmt: "~200 MB" },
  { label: "SD", res: "480p", vQuality: "480", sizeFmt: "~100 MB" },
  { label: "Low", res: "360p", vQuality: "360", sizeFmt: "~60 MB" }
];
const TIKTOK_QUALITIES = [
  { label: "No Watermark", res: "HD", vQuality: "720", sizeFmt: "~20 MB" },
  { label: "With Watermark", res: "SD", vQuality: "480", sizeFmt: "~15 MB" },
  {
    label: "Audio Only",
    res: "MP3",
    vQuality: "128",
    isAudio: true,
    sizeFmt: "~3 MB"
  }
];
const QUALITY_BADGE = {
  "1080p": "bg-cyan-500/15 text-cyan-400 border-cyan-500/30",
  "720p": "bg-emerald-500/15 text-emerald-400 border-emerald-500/30",
  "480p": "bg-amber-500/15 text-amber-400 border-amber-500/30",
  "360p": "bg-muted/60 text-muted-foreground border-border",
  HD: "bg-emerald-500/15 text-emerald-400 border-emerald-500/30",
  SD: "bg-amber-500/15 text-amber-400 border-amber-500/30",
  Best: "bg-violet-500/15 text-violet-400 border-violet-500/30",
  MP3: "bg-rose-500/15 text-rose-400 border-rose-500/30"
};
function DownloadPageInline({
  videoId,
  title,
  platform = "youtube",
  thumbnailUrl,
  channelTitle = "",
  duration = "",
  isSubscribed = false,
  onBack,
  onClose
}) {
  var _a;
  const { isRTL } = useTranslation();
  const [loadingRes, setLoadingRes] = reactExports.useState(null);
  const [successRes, setSuccessRes] = reactExports.useState(null);
  const [errorRes, setErrorRes] = reactExports.useState(null);
  const [savingOffline, setSavingOffline] = reactExports.useState(null);
  const [savedOfflineRes, setSavedOfflineRes] = reactExports.useState(null);
  const [progress, setProgress] = reactExports.useState(
    {}
  );
  const [downloadInfo, setDownloadInfo] = reactExports.useState(
    () => getDownloadLimitInfo()
  );
  const [copied, setCopied] = reactExports.useState(false);
  const [debugLog, setDebugLog] = reactExports.useState([]);
  const [showDebug, setShowDebug] = reactExports.useState(false);
  const [detailedError, setDetailedError] = reactExports.useState(null);
  reactExports.useEffect(() => {
    setDownloadInfo(getDownloadLimitInfo());
  }, []);
  const qualityMap = {
    youtube: YT_QUALITIES,
    vimeo: VIMEO_QUALITIES,
    tiktok: TIKTOK_QUALITIES
  };
  const qualities = qualityMap[platform] ?? YT_QUALITIES;
  const watchUrl = buildWatchUrl(platform, videoId);
  const thumb = thumbnailUrl ?? (platform === "youtube" ? `https://img.youtube.com/vi/${videoId}/hqdefault.jpg` : null);
  async function handleDownload(opt) {
    if (loadingRes) return;
    if (!isSubscribed) {
      const info = getDownloadLimitInfo();
      if (info.remaining <= 0) {
        ue.error(
          isRTL ? "لقد وصلت إلى الحد اليومي للتحميل." : "Daily download limit reached. Subscribe for unlimited!",
          { duration: 5e3 }
        );
        return;
      }
    }
    setLoadingRes(opt.res);
    setErrorRes(null);
    setSuccessRes(null);
    setDetailedError(null);
    setDebugLog([]);
    setShowDebug(false);
    setProgress((p) => ({
      ...p,
      [opt.res]: { stage: "idle", percent: 0 }
    }));
    try {
      const FALLBACK_KEY = "54ca2c15e4mshe644cf482a6a4edp19b3fajsn6ab22879e0db";
      const downloadKey = getDownloadRapidApiKey();
      const rapidApiKey = downloadKey || FALLBACK_KEY;
      const lowerPlatform = platform.toLowerCase();
      if (lowerPlatform === "tiktok") {
        const msg = isRTL ? "هذا الفيديو متاح للبث فقط — التحميل غير متاح على هذه المنصة" : "This video is stream-only — download not available for this platform";
        setErrorRes(opt.res);
        setDetailedError(msg);
        setDebugLog(getDownloadDebugLog());
        setShowDebug(true);
        ue.error(msg, { duration: 8e3 });
        setTimeout(() => setErrorRes(null), 6e3);
        return;
      }
      setProgress((p) => ({
        ...p,
        [opt.res]: { stage: "fetching", percent: 5 }
      }));
      const directUrl = await getVideoDownloadUrl(
        videoId,
        lowerPlatform,
        opt.vQuality,
        rapidApiKey
      );
      if (!directUrl) {
        const msg = isRTL ? "تعذّر الحصول على رابط التحميل — قد يكون الفيديو محميًا أو غير متاح" : "Could not get download URL — video may be protected or unavailable";
        setErrorRes(opt.res);
        setDetailedError(msg);
        setDebugLog(getDownloadDebugLog());
        setShowDebug(true);
        ue.error(msg, { duration: 8e3 });
        setTimeout(() => setErrorRes(null), 6e3);
        return;
      }
      const filename = `${(title ?? videoId).replace(/[^a-z0-9\u0600-\u06FF\s-]/gi, "").slice(0, 60)}_${opt.res}.${opt.isAudio ? "mp3" : "mp4"}`;
      await downloadVideo(
        directUrl,
        filename,
        rapidApiKey,
        (prog) => setProgress((p) => ({ ...p, [opt.res]: prog }))
      );
      const meta = {
        videoId,
        title: title ?? videoId,
        thumbnailUrl: thumb ?? "",
        viewCount: "0",
        duration,
        channelTitle,
        publishedAt: (/* @__PURE__ */ new Date()).toISOString(),
        platform
      };
      await saveVideo(meta);
      recordDownloadLocal();
      setDownloadInfo(getDownloadLimitInfo());
      setSuccessRes(opt.res);
      setTimeout(() => setSuccessRes(null), 4e3);
      ue.success(
        isRTL ? "✓ تم حفظ الفيديو على جهازك" : "✓ Video saved to your device",
        { duration: 3e3 }
      );
    } catch (err) {
      const msg = err instanceof Error ? err.message : String(err);
      setErrorRes(opt.res);
      setDetailedError(msg);
      setDebugLog(getDownloadDebugLog());
      setShowDebug(true);
      setTimeout(() => setErrorRes(null), 4e3);
    } finally {
      setLoadingRes(null);
      setProgress((p) => {
        const n = { ...p };
        delete n[opt.res];
        return n;
      });
    }
  }
  async function handleSaveOffline(opt) {
    if (savingOffline) return;
    setSavingOffline(opt.res);
    setProgress((p) => ({
      ...p,
      [`off_${opt.res}`]: { stage: "idle", percent: 0 }
    }));
    try {
      const FALLBACK_KEY = "54ca2c15e4mshe644cf482a6a4edp19b3fajsn6ab22879e0db";
      const downloadKey = getDownloadRapidApiKey();
      const rapidApiKey = downloadKey || FALLBACK_KEY;
      const lowerPlatform = platform.toLowerCase();
      if (lowerPlatform === "tiktok") {
        ue.error(
          isRTL ? "التحميل بدون إنترنت غير متاح لهذه المنصة" : "Offline save not available for this platform",
          { duration: 5e3 }
        );
        return;
      }
      const directUrl = await getVideoDownloadUrl(
        videoId,
        lowerPlatform,
        opt.vQuality,
        rapidApiKey
      );
      if (!directUrl) {
        ue.error(
          isRTL ? "تعذّر الحصول على رابط التحميل" : "Could not get download URL",
          { duration: 5e3 }
        );
        return;
      }
      const filename = `${(title ?? videoId).replace(/[^a-z0-9\u0600-\u06FF\s-]/gi, "").slice(0, 60)}_${opt.res}.${opt.isAudio ? "mp3" : "mp4"}`;
      const response = await fetch(directUrl);
      const blob = await response.blob();
      const arrayBuf = await blob.arrayBuffer();
      triggerBlobDownload(
        URL.createObjectURL(blob),
        filename
      );
      const meta = {
        videoId,
        title: title ?? videoId,
        thumbnailUrl: thumb ?? "",
        viewCount: "0",
        duration,
        channelTitle,
        publishedAt: (/* @__PURE__ */ new Date()).toISOString(),
        platform
      };
      await saveVideo(meta, arrayBuf);
      console.log("VIDEO SAVED TO INDEXEDDB", meta.videoId);
      setSavedOfflineRes(opt.res);
      setTimeout(() => setSavedOfflineRes(null), 4e3);
      ue.success(
        isRTL ? "✓ تم حفظ الفيديو للمشاهدة بدون إنترنت" : "✓ Saved for offline viewing"
      );
    } catch {
      ue.error(
        isRTL ? "تعذّر الحفظ بدون إنترنت" : "Could not save for offline"
      );
    } finally {
      setSavingOffline(null);
      setProgress((p) => {
        const n = { ...p };
        delete n[`off_${opt.res}`];
        return n;
      });
    }
  }
  async function copyLink() {
    try {
      await navigator.clipboard.writeText(watchUrl);
      setCopied(true);
      setTimeout(() => setCopied(false), 2500);
      ue.success(isRTL ? "تم نسخ الرابط" : "Link copied");
    } catch {
      ue.error(isRTL ? "فشل النسخ" : "Copy failed");
    }
  }
  const limitReached = !isSubscribed && downloadInfo.remaining <= 0;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "bg-background", "data-ocid": "download_page_inline", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "bg-card border-b border-border px-4 py-3 flex items-center gap-3", children: [
      onBack && /* @__PURE__ */ jsxRuntimeExports.jsx(
        "button",
        {
          type: "button",
          onClick: onBack,
          className: "w-8 h-8 flex items-center justify-center rounded-lg hover:bg-muted/60 text-muted-foreground hover:text-foreground transition-colors",
          "aria-label": isRTL ? "رجوع" : "Back",
          "data-ocid": "download_page.back_button",
          children: /* @__PURE__ */ jsxRuntimeExports.jsx(ChevronLeft, { className: "h-5 w-5" })
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 min-w-0 flex-1", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "w-7 h-7 rounded-lg bg-cyan-500/10 border border-cyan-500/20 flex items-center justify-center shrink-0", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Download, { className: "h-3.5 w-3.5 text-cyan-400" }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "min-w-0", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm font-display font-semibold text-foreground", children: isRTL ? "تحميل الفيديو" : "Download Video" }),
          title && /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground truncate max-w-[200px]", children: title })
        ] })
      ] }),
      onClose && /* @__PURE__ */ jsxRuntimeExports.jsx(
        "button",
        {
          type: "button",
          onClick: onClose,
          className: "w-8 h-8 flex items-center justify-center rounded-lg hover:bg-muted/60 text-muted-foreground hover:text-foreground transition-colors ms-auto",
          "aria-label": "Close",
          "data-ocid": "download_page.close_button",
          children: /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-lg leading-none", children: "×" })
        }
      )
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "px-4 py-5 space-y-5", children: [
      thumb && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "rounded-xl overflow-hidden aspect-video bg-muted relative", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        "img",
        {
          src: thumb,
          alt: title ?? "Thumbnail",
          className: "w-full h-full object-cover"
        }
      ) }),
      !isSubscribed && /* @__PURE__ */ jsxRuntimeExports.jsxs(
        "div",
        {
          className: `flex items-center gap-3 px-3 py-2.5 rounded-xl border text-sm ${limitReached ? "bg-destructive/10 border-destructive/30" : downloadInfo.remaining <= 1 ? "bg-amber-500/10 border-amber-500/30" : "bg-muted/30 border-border"}`,
          "data-ocid": "download_page.limit_banner",
          children: [
            limitReached ? /* @__PURE__ */ jsxRuntimeExports.jsx(CircleAlert, { className: "h-4 w-4 text-destructive shrink-0" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Download, { className: "h-4 w-4 text-muted-foreground shrink-0" }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex-1 min-w-0", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                "p",
                {
                  className: `text-xs font-medium ${limitReached ? "text-destructive" : "text-foreground"}`,
                  children: isRTL ? limitReached ? "وصلت إلى الحد اليومي" : `لديك ${downloadInfo.remaining} تحميل متبقي اليوم` : limitReached ? "Daily limit reached" : `${downloadInfo.remaining} download${downloadInfo.remaining === 1 ? "" : "s"} remaining today`
                }
              ),
              /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground", children: isRTL ? `${downloadInfo.used} / ${downloadInfo.limit} تحميلات` : `${downloadInfo.used} / ${downloadInfo.limit} used` })
            ] }),
            limitReached && /* @__PURE__ */ jsxRuntimeExports.jsx(
              Button,
              {
                type: "button",
                size: "sm",
                className: "shrink-0 gradient-primary text-white border-0 text-xs h-7",
                "data-ocid": "download_page.upgrade_button",
                onClick: () => {
                  window.location.href = "/subscribe";
                },
                children: isRTL ? "اشترك" : "Subscribe"
              }
            )
          ]
        }
      ),
      !limitReached && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-2", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 mb-3", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Film, { className: "h-4 w-4 text-muted-foreground" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm font-semibold text-foreground", children: isRTL ? "اختر الجودة" : "Select Quality" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Badge,
            {
              className: `text-xs border ms-auto ${platform === "vimeo" ? "bg-sky-500/15 text-sky-400 border-sky-500/30" : platform === "tiktok" ? "bg-pink-500/15 text-pink-400 border-pink-500/30" : "bg-rose-500/15 text-rose-400 border-rose-500/30"}`,
              children: platform === "vimeo" ? "Vimeo" : platform === "tiktok" ? "TikTok" : "YouTube"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "space-y-2", "data-ocid": "download_page.quality_list", children: qualities.map((opt) => {
          const isLoading = loadingRes === opt.res;
          const isSuccess = successRes === opt.res;
          const isError = errorRes === opt.res;
          const isSavingOff = savingOffline === opt.res;
          const isSavedOff = savedOfflineRes === opt.res;
          const prog = progress[opt.res];
          const offProg = progress[`off_${opt.res}`];
          return /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "div",
            {
              className: `flex flex-col gap-2 px-3 py-2.5 rounded-xl border transition-all ${isSuccess || isSavedOff ? "border-emerald-500/40 bg-emerald-500/10" : isError ? "border-destructive/40 bg-destructive/10" : "border-border bg-muted/20 hover:bg-muted/40"}`,
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-3", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx(
                    Badge,
                    {
                      className: `text-xs border shrink-0 font-mono ${QUALITY_BADGE[opt.res] ?? "bg-muted/60 text-muted-foreground border-border"}`,
                      children: opt.res
                    }
                  ),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex-1 min-w-0", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm font-medium text-foreground", children: opt.label }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground", children: opt.sizeFmt })
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 shrink-0", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx(
                      "button",
                      {
                        type: "button",
                        disabled: !!loadingRes || !!savingOffline,
                        onClick: () => void handleSaveOffline(opt),
                        "aria-label": isRTL ? "حفظ بدون إنترنت" : "Save offline",
                        "data-ocid": `download_page.offline_button.${opt.res.toLowerCase()}`,
                        title: isRTL ? "حفظ للتشغيل بدون إنترنت" : "Save for offline",
                        className: `w-7 h-7 flex items-center justify-center rounded-lg border transition-colors disabled:opacity-40 ${isSavedOff ? "border-emerald-500/40 bg-emerald-500/20 text-emerald-400" : "border-border hover:bg-primary/10 hover:border-primary/40 hover:text-primary text-muted-foreground"}`,
                        children: isSavingOff ? /* @__PURE__ */ jsxRuntimeExports.jsx(LoaderCircle, { className: "h-3.5 w-3.5 animate-spin" }) : isSavedOff ? /* @__PURE__ */ jsxRuntimeExports.jsx(CircleCheck, { className: "h-3.5 w-3.5" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(WifiOff, { className: "h-3.5 w-3.5" })
                      }
                    ),
                    /* @__PURE__ */ jsxRuntimeExports.jsxs(
                      "button",
                      {
                        type: "button",
                        disabled: !!loadingRes,
                        onClick: () => void handleDownload(opt),
                        "data-ocid": `download_page.quality_button.${opt.res.toLowerCase()}`,
                        className: `flex items-center gap-1.5 px-3 py-1.5 rounded-lg text-sm font-medium transition-all disabled:opacity-50 ${isSuccess ? "bg-emerald-500/20 text-emerald-400 border border-emerald-500/40" : isError ? "bg-destructive/20 text-destructive border border-destructive/40" : "bg-cyan-500/10 text-cyan-400 border border-cyan-500/30 hover:bg-cyan-500/20"}`,
                        children: [
                          isLoading ? /* @__PURE__ */ jsxRuntimeExports.jsx(LoaderCircle, { className: "h-3.5 w-3.5 animate-spin" }) : isSuccess ? /* @__PURE__ */ jsxRuntimeExports.jsx(CircleCheck, { className: "h-3.5 w-3.5" }) : isError ? /* @__PURE__ */ jsxRuntimeExports.jsx(CircleAlert, { className: "h-3.5 w-3.5" }) : opt.isAudio ? /* @__PURE__ */ jsxRuntimeExports.jsx(Music, { className: "h-3.5 w-3.5" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Download, { className: "h-3.5 w-3.5" }),
                          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: isLoading ? isRTL ? "جارى..." : "Fetching..." : isSuccess ? isRTL ? "تم!" : "Saved!" : isError ? isRTL ? "خطأ" : "Error" : isRTL ? "تحميل" : "Download" })
                        ]
                      }
                    )
                  ] })
                ] }),
                prog && isLoading && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "w-full h-1.5 bg-muted/40 rounded-full overflow-hidden", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
                  "div",
                  {
                    className: "h-full bg-cyan-500 rounded-full transition-all duration-300",
                    style: {
                      width: `${prog.percent > 0 ? prog.percent : 15}%`
                    }
                  }
                ) }),
                offProg && isSavingOff && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "w-full h-1.5 bg-muted/40 rounded-full overflow-hidden", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
                  "div",
                  {
                    className: "h-full bg-primary rounded-full transition-all duration-300",
                    style: {
                      width: `${offProg.percent > 0 ? offProg.percent : 15}%`
                    }
                  }
                ) })
              ]
            },
            opt.res
          );
        }) })
      ] }),
      limitReached && /* @__PURE__ */ jsxRuntimeExports.jsxs(
        "div",
        {
          className: "text-center py-6 space-y-3",
          "data-ocid": "download_page.limit_empty_state",
          children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(CircleAlert, { className: "h-10 w-10 mx-auto text-destructive" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "font-display font-semibold text-foreground", children: isRTL ? "تجاوزت الحد اليومي" : "Daily limit reached" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm text-muted-foreground", children: isRTL ? "المستخدمون المجانيون: 3 تحميلات/يوم" : "Free users: 3 downloads per day" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              Button,
              {
                type: "button",
                className: "gradient-primary text-white border-0",
                "data-ocid": "download_page.subscribe_button",
                onClick: () => {
                  window.location.href = "/subscribe";
                },
                children: isRTL ? "اشترك الآن" : "Subscribe Now"
              }
            )
          ]
        }
      ),
      detailedError && /* @__PURE__ */ jsxRuntimeExports.jsxs(
        "div",
        {
          className: "rounded-xl border border-destructive/40 bg-destructive/10 p-4 space-y-2",
          "data-ocid": "download_page.error_diagnostic",
          children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 text-destructive", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(CircleAlert, { className: "h-4 w-4" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm font-semibold", children: isRTL ? "❌ فشل التحميل" : "❌ Download Failed" })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-1 text-xs text-foreground/90", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("p", { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-semibold", children: isRTL ? "السبب:" : "Reason:" }),
                " ",
                detailedError
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("p", { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-semibold", children: isRTL ? "الخطوة:" : "Step:" }),
                " ",
                ((_a = debugLog.find((e) => e.status === "error")) == null ? void 0 : _a.step) ?? "unknown"
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("p", { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-semibold", children: isRTL ? "المنصة:" : "Platform:" }),
                " ",
                platform
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("p", { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-semibold", children: isRTL ? "معرف الفيديو:" : "Video ID:" }),
                " ",
                videoId
              ] })
            ] })
          ]
        }
      ),
      showDebug && debugLog.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "rounded-xl border border-border bg-muted/30 overflow-hidden", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 px-4 py-2.5 text-xs font-medium text-muted-foreground", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Terminal, { className: "h-3.5 w-3.5" }),
          isRTL ? "سجل التشخيص" : "Debug Log",
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "ms-auto text-[10px] bg-muted px-1.5 py-0.5 rounded", children: debugLog.length })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "px-4 pb-3 space-y-1.5 max-h-64 overflow-y-auto", children: debugLog.map((entry) => /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "div",
          {
            className: `text-[11px] font-mono leading-relaxed rounded px-2 py-1 ${entry.status === "error" ? "bg-destructive/10 text-destructive" : entry.status === "warning" ? "bg-amber-500/10 text-amber-400" : entry.status === "success" ? "bg-emerald-500/10 text-emerald-400" : "bg-muted/40 text-muted-foreground"}`,
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "opacity-60", children: entry.timestamp.slice(11, 19) }),
              " ",
              /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "font-semibold", children: [
                "[",
                entry.step,
                "]"
              ] }),
              " ",
              entry.message,
              entry.details && /* @__PURE__ */ jsxRuntimeExports.jsx("pre", { className: "mt-1 text-[10px] opacity-80 whitespace-pre-wrap", children: JSON.stringify(entry.details, null, 2) })
            ]
          },
          `${entry.timestamp}-${entry.step}-${entry.message}`
        )) })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-2 border-t border-border pt-4", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground font-medium uppercase tracking-wide", children: isRTL ? "خيارات أخرى" : "Other Options" }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex gap-2", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "button",
            {
              type: "button",
              onClick: () => void copyLink(),
              "data-ocid": "download_page.copy_link_button",
              className: "flex-1 flex items-center justify-center gap-2 py-2 rounded-xl border border-border bg-muted/20 hover:bg-muted/40 text-sm text-foreground transition-colors",
              children: [
                copied ? /* @__PURE__ */ jsxRuntimeExports.jsx(CircleCheck, { className: "h-4 w-4 text-emerald-400" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(ClipboardCopy, { className: "h-4 w-4 text-muted-foreground" }),
                isRTL ? "نسخ الرابط" : "Copy Link"
              ]
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "a",
            {
              href: watchUrl,
              target: "_blank",
              rel: "noopener noreferrer",
              "data-ocid": "download_page.open_external_link",
              className: "flex-1 flex items-center justify-center gap-2 py-2 rounded-xl border border-border bg-muted/20 hover:bg-muted/40 text-sm text-foreground transition-colors",
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(ExternalLink, { className: "h-4 w-4 text-muted-foreground" }),
                isRTL ? "فتح خارجيًا" : "Open Externally"
              ]
            }
          )
        ] })
      ] })
    ] })
  ] });
}
function getPlatformLabel(platform) {
  switch (platform) {
    case "vimeo":
      return {
        label: "Vimeo",
        color: "bg-sky-500/15 text-sky-400 border-sky-500/30"
      };
    case "tiktok":
      return {
        label: "TikTok",
        color: "bg-pink-500/15 text-pink-400 border-pink-500/30"
      };
    default:
      return {
        label: "YouTube",
        color: "bg-rose-500/15 text-rose-400 border-rose-500/30"
      };
  }
}
function DownloadModal({
  open,
  onClose,
  videoId,
  title,
  platform
}) {
  const { isRTL } = useTranslation();
  const [showDownloadPage, setShowDownloadPage] = reactExports.useState(false);
  const meta = getPlatformLabel(platform);
  if (showDownloadPage) {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(Dialog, { open, onOpenChange: (v) => !v && onClose(), children: /* @__PURE__ */ jsxRuntimeExports.jsx(
      DialogContent,
      {
        className: "max-w-2xl w-full mx-2 p-0 bg-background border-border shadow-2xl overflow-hidden max-h-[92vh] overflow-y-auto",
        "data-ocid": "download.dialog",
        children: /* @__PURE__ */ jsxRuntimeExports.jsx(
          DownloadPageInline,
          {
            videoId,
            title,
            platform,
            onBack: () => setShowDownloadPage(false),
            onClose
          }
        )
      }
    ) });
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Dialog, { open, onOpenChange: (v) => !v && onClose(), children: /* @__PURE__ */ jsxRuntimeExports.jsxs(
    DialogContent,
    {
      className: "max-w-sm w-full mx-4 bg-card border-border shadow-2xl",
      "data-ocid": "download.dialog",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(DialogHeader, { children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-start justify-between gap-3", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2.5 min-w-0", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "w-9 h-9 rounded-xl bg-cyan-500/10 border border-cyan-500/20 flex items-center justify-center shrink-0", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Download, { className: "h-4.5 w-4.5 text-cyan-400" }) }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "min-w-0", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(DialogTitle, { className: "text-base font-display font-semibold text-foreground leading-tight", children: isRTL ? "تحميل الفيديو" : "Download Video" }),
              title && /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground mt-0.5 truncate max-w-[200px]", children: title })
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Button,
            {
              type: "button",
              variant: "ghost",
              size: "icon",
              className: "h-8 w-8 shrink-0 text-muted-foreground hover:text-foreground -mt-1 -me-1",
              onClick: onClose,
              "aria-label": "Close",
              "data-ocid": "download.close_button",
              children: /* @__PURE__ */ jsxRuntimeExports.jsx(X, { className: "h-4 w-4" })
            }
          )
        ] }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 -mt-1 mb-1", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Film, { className: "h-3.5 w-3.5 text-muted-foreground" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-xs text-muted-foreground", children: isRTL ? "المنصة:" : "Platform:" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { className: cn("text-xs font-medium border", meta.color), children: meta.label })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(
          Button,
          {
            type: "button",
            onClick: () => setShowDownloadPage(true),
            className: "w-full gradient-primary text-white border-0 gap-2",
            "data-ocid": "download.open_download_page_button",
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Download, { className: "h-4 w-4" }),
              isRTL ? "اختر الجودة وحمّل" : "Choose Quality & Download"
            ]
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground/70 leading-relaxed pt-1 border-t border-border", children: isRTL ? "يتم جلب رابط التحميل مباشرةً. تأكد من الالتزام بشروط خدمة المنصة." : "Download link is fetched directly. Please comply with the platform's terms of service." })
      ]
    }
  ) });
}
function formatCount(n) {
  if (n >= 1e6) return `${(n / 1e6).toFixed(1)}M`;
  if (n >= 1e3) return `${(n / 1e3).toFixed(1)}K`;
  return n.toString();
}
function isDirectDownloadUrl(url) {
  if (!url) return false;
  try {
    const parsed = new URL(url);
    const host = parsed.hostname.toLowerCase();
    const isExternal = host.includes("youtube") || host.includes("youtu.be") || host.includes("vimeo");
    return !isExternal;
  } catch {
    return true;
  }
}
async function handleVideoDownload(platform, videoId, videoUrl, title, t, isRTL = false) {
  var _a;
  const directUrl = videoUrl && isDirectDownloadUrl(videoUrl) ? videoUrl : null;
  if (directUrl) {
    const toastId = ue.loading(t("download.preparing"));
    try {
      const response = await fetch(directUrl);
      if (!response.ok) throw new Error("Network response not ok");
      const blob = await response.blob();
      const blobUrl = URL.createObjectURL(blob);
      const a = document.createElement("a");
      a.href = blobUrl;
      const ext = blob.type.split("/")[1] || ((_a = directUrl.split(".").pop()) == null ? void 0 : _a.split("?")[0]) || "mp4";
      a.download = `${title.replace(/[^a-z0-9]/gi, "_").slice(0, 60)}.${ext}`;
      document.body.appendChild(a);
      a.click();
      document.body.removeChild(a);
      setTimeout(() => URL.revokeObjectURL(blobUrl), 1e4);
      ue.success(t("download.success"), { id: toastId });
    } catch {
      ue.error(t("download.failed"), { id: toastId });
    }
    return;
  }
  const fallbackUrl = platform === "vimeo" ? `https://vimeo.com/${videoId}` : `https://www.youtube.com/watch?v=${videoId}`;
  ue.info(
    isRTL ? "يرجى استخدام زر التحميل في البطاقة" : "Use the download button on the video card",
    { duration: 4e3 }
  );
  try {
    await navigator.clipboard.writeText(fallbackUrl);
    ue.success(
      isRTL ? "تم نسخ رابط الفيديو" : "Video link copied to clipboard",
      { duration: 3e3 }
    );
  } catch {
  }
}
function SocialBar({
  videoId,
  videoUrl,
  platform,
  likeCount = 0,
  dislikeCount = 0,
  commentCount = 0,
  userReaction,
  onLike,
  onDislike,
  onComment,
  onShare,
  compact = false,
  index,
  className,
  videoTitle,
  onAddToPlaylist
}) {
  const { t, isRTL } = useTranslation();
  const [downloading, setDownloading] = reactExports.useState(false);
  const [showDownloadModal, setShowDownloadModal] = reactExports.useState(false);
  const ocidSuffix = index !== void 0 ? `.${index + 1}` : "";
  const isExternal = !platform || platform === "youtube" || platform === "vimeo" || platform === "tiktok" || platform === "kwai";
  const directUrl = videoUrl && !isExternal ? videoUrl : null;
  const onDownload = async (e) => {
    e.stopPropagation();
    if (downloading) return;
    if (isExternal || !directUrl) {
      setShowDownloadModal(true);
      return;
    }
    setDownloading(true);
    try {
      await handleVideoDownload(
        platform,
        videoId,
        directUrl ?? void 0,
        videoTitle ?? videoId,
        t
      );
    } finally {
      setDownloading(false);
    }
  };
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      className: cn(
        "flex items-center gap-1",
        compact ? "gap-0.5" : "gap-1",
        className
      ),
      dir: isRTL ? "rtl" : "ltr",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "button",
          {
            type: "button",
            "data-ocid": `social.like_button${ocidSuffix}`,
            onClick: (e) => {
              e.stopPropagation();
              onLike == null ? void 0 : onLike();
            },
            "aria-label": isRTL ? "إعجاب" : "Like",
            className: cn(
              "social-button rounded-full transition-all duration-200 active:scale-95",
              compact ? "px-2 py-1 text-xs gap-1" : "px-3 py-2 text-sm gap-1.5",
              userReaction === "like" ? "bg-green-500/20 text-green-400 ring-1 ring-green-500/40" : "hover:bg-green-500/10 text-muted-foreground hover:text-green-400"
            ),
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                ThumbsUp,
                {
                  className: cn(
                    compact ? "h-3.5 w-3.5" : "h-4 w-4",
                    userReaction === "like" && "fill-green-400"
                  )
                }
              ),
              likeCount > 0 && /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-mono font-medium tabular-nums", children: formatCount(likeCount) })
            ]
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "button",
          {
            type: "button",
            "data-ocid": `social.dislike_button${ocidSuffix}`,
            onClick: (e) => {
              e.stopPropagation();
              onDislike == null ? void 0 : onDislike();
            },
            "aria-label": isRTL ? "عدم إعجاب" : "Dislike",
            className: cn(
              "social-button rounded-full transition-all duration-200 active:scale-95",
              compact ? "px-2 py-1 text-xs gap-1" : "px-3 py-2 text-sm gap-1.5",
              userReaction === "dislike" ? "bg-destructive/20 text-destructive ring-1 ring-destructive/40" : "hover:bg-destructive/10 text-muted-foreground hover:text-destructive"
            ),
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                ThumbsDown,
                {
                  className: cn(
                    compact ? "h-3.5 w-3.5" : "h-4 w-4",
                    userReaction === "dislike" && "fill-destructive"
                  )
                }
              ),
              dislikeCount > 0 && /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-mono font-medium tabular-nums", children: formatCount(dislikeCount) })
            ]
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "button",
          {
            type: "button",
            "data-ocid": `social.comment_button${ocidSuffix}`,
            onClick: (e) => {
              e.stopPropagation();
              onComment == null ? void 0 : onComment();
            },
            "aria-label": isRTL ? "التعليقات" : "Comments",
            className: cn(
              "social-button rounded-full transition-all duration-200 hover:bg-primary/10 hover:text-primary text-muted-foreground active:scale-95",
              compact ? "px-2 py-1 text-xs gap-1" : "px-3 py-2 text-sm gap-1.5"
            ),
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(MessageCircle, { className: compact ? "h-3.5 w-3.5" : "h-4 w-4" }),
              commentCount > 0 && /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-mono font-medium tabular-nums", children: formatCount(commentCount) })
            ]
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "button",
          {
            type: "button",
            "data-ocid": `social.share_button${ocidSuffix}`,
            onClick: (e) => {
              e.stopPropagation();
              onShare == null ? void 0 : onShare();
            },
            "aria-label": isRTL ? "مشاركة" : "Share",
            className: cn(
              "social-button rounded-full transition-all duration-200 hover:bg-primary/10 hover:text-primary text-muted-foreground active:scale-95",
              compact ? "px-2 py-1 text-xs gap-1" : "px-3 py-2 text-sm gap-1.5"
            ),
            children: /* @__PURE__ */ jsxRuntimeExports.jsx(Share2, { className: compact ? "h-3.5 w-3.5" : "h-4 w-4" })
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "button",
          {
            type: "button",
            "data-ocid": `social.download_button${ocidSuffix}`,
            onClick: onDownload,
            disabled: downloading,
            "aria-label": isRTL ? "تحميل" : "Download",
            className: cn(
              "social-button rounded-full transition-all duration-200 hover:bg-cyan-500/10 hover:text-cyan-400 text-muted-foreground active:scale-95 disabled:opacity-50 disabled:cursor-not-allowed",
              compact ? "px-2 py-1 text-xs gap-1" : "px-3 py-2 text-sm gap-1.5"
            ),
            children: downloading ? /* @__PURE__ */ jsxRuntimeExports.jsx(
              LoaderCircle,
              {
                className: cn("animate-spin", compact ? "h-3.5 w-3.5" : "h-4 w-4")
              }
            ) : /* @__PURE__ */ jsxRuntimeExports.jsx(Download, { className: compact ? "h-3.5 w-3.5" : "h-4 w-4" })
          }
        ),
        onAddToPlaylist && /* @__PURE__ */ jsxRuntimeExports.jsx(
          "button",
          {
            type: "button",
            "data-ocid": `social.add_to_playlist_button${ocidSuffix}`,
            onClick: (e) => {
              e.stopPropagation();
              onAddToPlaylist();
            },
            "aria-label": isRTL ? "إضافة إلى قائمة" : "Add to playlist",
            className: cn(
              "social-button rounded-full transition-all duration-200 hover:bg-primary/10 hover:text-primary text-muted-foreground active:scale-95",
              compact ? "px-2 py-1 text-xs gap-1" : "px-3 py-2 text-sm gap-1.5"
            ),
            children: /* @__PURE__ */ jsxRuntimeExports.jsx(BookmarkPlus, { className: compact ? "h-3.5 w-3.5" : "h-4 w-4" })
          }
        ),
        isExternal && /* @__PURE__ */ jsxRuntimeExports.jsx(
          DownloadModal,
          {
            open: showDownloadModal,
            onClose: () => setShowDownloadModal(false),
            videoId,
            title: videoTitle,
            platform,
            videoUrl
          }
        )
      ]
    }
  );
}
function useStateMachine(initialState, machine) {
  return reactExports.useReducer((state, event) => {
    const nextState = machine[state][event];
    return nextState ?? state;
  }, initialState);
}
var SCROLL_AREA_NAME = "ScrollArea";
var [createScrollAreaContext] = createContextScope(SCROLL_AREA_NAME);
var [ScrollAreaProvider, useScrollAreaContext] = createScrollAreaContext(SCROLL_AREA_NAME);
var ScrollArea$1 = reactExports.forwardRef(
  (props, forwardedRef) => {
    const {
      __scopeScrollArea,
      type = "hover",
      dir,
      scrollHideDelay = 600,
      ...scrollAreaProps
    } = props;
    const [scrollArea, setScrollArea] = reactExports.useState(null);
    const [viewport, setViewport] = reactExports.useState(null);
    const [content, setContent] = reactExports.useState(null);
    const [scrollbarX, setScrollbarX] = reactExports.useState(null);
    const [scrollbarY, setScrollbarY] = reactExports.useState(null);
    const [cornerWidth, setCornerWidth] = reactExports.useState(0);
    const [cornerHeight, setCornerHeight] = reactExports.useState(0);
    const [scrollbarXEnabled, setScrollbarXEnabled] = reactExports.useState(false);
    const [scrollbarYEnabled, setScrollbarYEnabled] = reactExports.useState(false);
    const composedRefs = useComposedRefs(forwardedRef, (node) => setScrollArea(node));
    const direction = useDirection(dir);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      ScrollAreaProvider,
      {
        scope: __scopeScrollArea,
        type,
        dir: direction,
        scrollHideDelay,
        scrollArea,
        viewport,
        onViewportChange: setViewport,
        content,
        onContentChange: setContent,
        scrollbarX,
        onScrollbarXChange: setScrollbarX,
        scrollbarXEnabled,
        onScrollbarXEnabledChange: setScrollbarXEnabled,
        scrollbarY,
        onScrollbarYChange: setScrollbarY,
        scrollbarYEnabled,
        onScrollbarYEnabledChange: setScrollbarYEnabled,
        onCornerWidthChange: setCornerWidth,
        onCornerHeightChange: setCornerHeight,
        children: /* @__PURE__ */ jsxRuntimeExports.jsx(
          Primitive.div,
          {
            dir: direction,
            ...scrollAreaProps,
            ref: composedRefs,
            style: {
              position: "relative",
              // Pass corner sizes as CSS vars to reduce re-renders of context consumers
              ["--radix-scroll-area-corner-width"]: cornerWidth + "px",
              ["--radix-scroll-area-corner-height"]: cornerHeight + "px",
              ...props.style
            }
          }
        )
      }
    );
  }
);
ScrollArea$1.displayName = SCROLL_AREA_NAME;
var VIEWPORT_NAME = "ScrollAreaViewport";
var ScrollAreaViewport = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { __scopeScrollArea, children, nonce, ...viewportProps } = props;
    const context = useScrollAreaContext(VIEWPORT_NAME, __scopeScrollArea);
    const ref = reactExports.useRef(null);
    const composedRefs = useComposedRefs(forwardedRef, ref, context.onViewportChange);
    return /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        "style",
        {
          dangerouslySetInnerHTML: {
            __html: `[data-radix-scroll-area-viewport]{scrollbar-width:none;-ms-overflow-style:none;-webkit-overflow-scrolling:touch;}[data-radix-scroll-area-viewport]::-webkit-scrollbar{display:none}`
          },
          nonce
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        Primitive.div,
        {
          "data-radix-scroll-area-viewport": "",
          ...viewportProps,
          ref: composedRefs,
          style: {
            /**
             * We don't support `visible` because the intention is to have at least one scrollbar
             * if this component is used and `visible` will behave like `auto` in that case
             * https://developer.mozilla.org/en-US/docs/Web/CSS/overflow#description
             *
             * We don't handle `auto` because the intention is for the native implementation
             * to be hidden if using this component. We just want to ensure the node is scrollable
             * so could have used either `scroll` or `auto` here. We picked `scroll` to prevent
             * the browser from having to work out whether to render native scrollbars or not,
             * we tell it to with the intention of hiding them in CSS.
             */
            overflowX: context.scrollbarXEnabled ? "scroll" : "hidden",
            overflowY: context.scrollbarYEnabled ? "scroll" : "hidden",
            ...props.style
          },
          children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { ref: context.onContentChange, style: { minWidth: "100%", display: "table" }, children })
        }
      )
    ] });
  }
);
ScrollAreaViewport.displayName = VIEWPORT_NAME;
var SCROLLBAR_NAME = "ScrollAreaScrollbar";
var ScrollAreaScrollbar = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { forceMount, ...scrollbarProps } = props;
    const context = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
    const { onScrollbarXEnabledChange, onScrollbarYEnabledChange } = context;
    const isHorizontal = props.orientation === "horizontal";
    reactExports.useEffect(() => {
      isHorizontal ? onScrollbarXEnabledChange(true) : onScrollbarYEnabledChange(true);
      return () => {
        isHorizontal ? onScrollbarXEnabledChange(false) : onScrollbarYEnabledChange(false);
      };
    }, [isHorizontal, onScrollbarXEnabledChange, onScrollbarYEnabledChange]);
    return context.type === "hover" ? /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollAreaScrollbarHover, { ...scrollbarProps, ref: forwardedRef, forceMount }) : context.type === "scroll" ? /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollAreaScrollbarScroll, { ...scrollbarProps, ref: forwardedRef, forceMount }) : context.type === "auto" ? /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollAreaScrollbarAuto, { ...scrollbarProps, ref: forwardedRef, forceMount }) : context.type === "always" ? /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollAreaScrollbarVisible, { ...scrollbarProps, ref: forwardedRef }) : null;
  }
);
ScrollAreaScrollbar.displayName = SCROLLBAR_NAME;
var ScrollAreaScrollbarHover = reactExports.forwardRef((props, forwardedRef) => {
  const { forceMount, ...scrollbarProps } = props;
  const context = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
  const [visible, setVisible] = reactExports.useState(false);
  reactExports.useEffect(() => {
    const scrollArea = context.scrollArea;
    let hideTimer = 0;
    if (scrollArea) {
      const handlePointerEnter = () => {
        window.clearTimeout(hideTimer);
        setVisible(true);
      };
      const handlePointerLeave = () => {
        hideTimer = window.setTimeout(() => setVisible(false), context.scrollHideDelay);
      };
      scrollArea.addEventListener("pointerenter", handlePointerEnter);
      scrollArea.addEventListener("pointerleave", handlePointerLeave);
      return () => {
        window.clearTimeout(hideTimer);
        scrollArea.removeEventListener("pointerenter", handlePointerEnter);
        scrollArea.removeEventListener("pointerleave", handlePointerLeave);
      };
    }
  }, [context.scrollArea, context.scrollHideDelay]);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Presence, { present: forceMount || visible, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
    ScrollAreaScrollbarAuto,
    {
      "data-state": visible ? "visible" : "hidden",
      ...scrollbarProps,
      ref: forwardedRef
    }
  ) });
});
var ScrollAreaScrollbarScroll = reactExports.forwardRef((props, forwardedRef) => {
  const { forceMount, ...scrollbarProps } = props;
  const context = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
  const isHorizontal = props.orientation === "horizontal";
  const debounceScrollEnd = useDebounceCallback(() => send("SCROLL_END"), 100);
  const [state, send] = useStateMachine("hidden", {
    hidden: {
      SCROLL: "scrolling"
    },
    scrolling: {
      SCROLL_END: "idle",
      POINTER_ENTER: "interacting"
    },
    interacting: {
      SCROLL: "interacting",
      POINTER_LEAVE: "idle"
    },
    idle: {
      HIDE: "hidden",
      SCROLL: "scrolling",
      POINTER_ENTER: "interacting"
    }
  });
  reactExports.useEffect(() => {
    if (state === "idle") {
      const hideTimer = window.setTimeout(() => send("HIDE"), context.scrollHideDelay);
      return () => window.clearTimeout(hideTimer);
    }
  }, [state, context.scrollHideDelay, send]);
  reactExports.useEffect(() => {
    const viewport = context.viewport;
    const scrollDirection = isHorizontal ? "scrollLeft" : "scrollTop";
    if (viewport) {
      let prevScrollPos = viewport[scrollDirection];
      const handleScroll = () => {
        const scrollPos = viewport[scrollDirection];
        const hasScrollInDirectionChanged = prevScrollPos !== scrollPos;
        if (hasScrollInDirectionChanged) {
          send("SCROLL");
          debounceScrollEnd();
        }
        prevScrollPos = scrollPos;
      };
      viewport.addEventListener("scroll", handleScroll);
      return () => viewport.removeEventListener("scroll", handleScroll);
    }
  }, [context.viewport, isHorizontal, send, debounceScrollEnd]);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Presence, { present: forceMount || state !== "hidden", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
    ScrollAreaScrollbarVisible,
    {
      "data-state": state === "hidden" ? "hidden" : "visible",
      ...scrollbarProps,
      ref: forwardedRef,
      onPointerEnter: composeEventHandlers(props.onPointerEnter, () => send("POINTER_ENTER")),
      onPointerLeave: composeEventHandlers(props.onPointerLeave, () => send("POINTER_LEAVE"))
    }
  ) });
});
var ScrollAreaScrollbarAuto = reactExports.forwardRef((props, forwardedRef) => {
  const context = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
  const { forceMount, ...scrollbarProps } = props;
  const [visible, setVisible] = reactExports.useState(false);
  const isHorizontal = props.orientation === "horizontal";
  const handleResize = useDebounceCallback(() => {
    if (context.viewport) {
      const isOverflowX = context.viewport.offsetWidth < context.viewport.scrollWidth;
      const isOverflowY = context.viewport.offsetHeight < context.viewport.scrollHeight;
      setVisible(isHorizontal ? isOverflowX : isOverflowY);
    }
  }, 10);
  useResizeObserver(context.viewport, handleResize);
  useResizeObserver(context.content, handleResize);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Presence, { present: forceMount || visible, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
    ScrollAreaScrollbarVisible,
    {
      "data-state": visible ? "visible" : "hidden",
      ...scrollbarProps,
      ref: forwardedRef
    }
  ) });
});
var ScrollAreaScrollbarVisible = reactExports.forwardRef((props, forwardedRef) => {
  const { orientation = "vertical", ...scrollbarProps } = props;
  const context = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
  const thumbRef = reactExports.useRef(null);
  const pointerOffsetRef = reactExports.useRef(0);
  const [sizes, setSizes] = reactExports.useState({
    content: 0,
    viewport: 0,
    scrollbar: { size: 0, paddingStart: 0, paddingEnd: 0 }
  });
  const thumbRatio = getThumbRatio(sizes.viewport, sizes.content);
  const commonProps = {
    ...scrollbarProps,
    sizes,
    onSizesChange: setSizes,
    hasThumb: Boolean(thumbRatio > 0 && thumbRatio < 1),
    onThumbChange: (thumb) => thumbRef.current = thumb,
    onThumbPointerUp: () => pointerOffsetRef.current = 0,
    onThumbPointerDown: (pointerPos) => pointerOffsetRef.current = pointerPos
  };
  function getScrollPosition(pointerPos, dir) {
    return getScrollPositionFromPointer(pointerPos, pointerOffsetRef.current, sizes, dir);
  }
  if (orientation === "horizontal") {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      ScrollAreaScrollbarX,
      {
        ...commonProps,
        ref: forwardedRef,
        onThumbPositionChange: () => {
          if (context.viewport && thumbRef.current) {
            const scrollPos = context.viewport.scrollLeft;
            const offset = getThumbOffsetFromScroll(scrollPos, sizes, context.dir);
            thumbRef.current.style.transform = `translate3d(${offset}px, 0, 0)`;
          }
        },
        onWheelScroll: (scrollPos) => {
          if (context.viewport) context.viewport.scrollLeft = scrollPos;
        },
        onDragScroll: (pointerPos) => {
          if (context.viewport) {
            context.viewport.scrollLeft = getScrollPosition(pointerPos, context.dir);
          }
        }
      }
    );
  }
  if (orientation === "vertical") {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      ScrollAreaScrollbarY,
      {
        ...commonProps,
        ref: forwardedRef,
        onThumbPositionChange: () => {
          if (context.viewport && thumbRef.current) {
            const scrollPos = context.viewport.scrollTop;
            const offset = getThumbOffsetFromScroll(scrollPos, sizes);
            thumbRef.current.style.transform = `translate3d(0, ${offset}px, 0)`;
          }
        },
        onWheelScroll: (scrollPos) => {
          if (context.viewport) context.viewport.scrollTop = scrollPos;
        },
        onDragScroll: (pointerPos) => {
          if (context.viewport) context.viewport.scrollTop = getScrollPosition(pointerPos);
        }
      }
    );
  }
  return null;
});
var ScrollAreaScrollbarX = reactExports.forwardRef((props, forwardedRef) => {
  const { sizes, onSizesChange, ...scrollbarProps } = props;
  const context = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
  const [computedStyle, setComputedStyle] = reactExports.useState();
  const ref = reactExports.useRef(null);
  const composeRefs = useComposedRefs(forwardedRef, ref, context.onScrollbarXChange);
  reactExports.useEffect(() => {
    if (ref.current) setComputedStyle(getComputedStyle(ref.current));
  }, [ref]);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    ScrollAreaScrollbarImpl,
    {
      "data-orientation": "horizontal",
      ...scrollbarProps,
      ref: composeRefs,
      sizes,
      style: {
        bottom: 0,
        left: context.dir === "rtl" ? "var(--radix-scroll-area-corner-width)" : 0,
        right: context.dir === "ltr" ? "var(--radix-scroll-area-corner-width)" : 0,
        ["--radix-scroll-area-thumb-width"]: getThumbSize(sizes) + "px",
        ...props.style
      },
      onThumbPointerDown: (pointerPos) => props.onThumbPointerDown(pointerPos.x),
      onDragScroll: (pointerPos) => props.onDragScroll(pointerPos.x),
      onWheelScroll: (event, maxScrollPos) => {
        if (context.viewport) {
          const scrollPos = context.viewport.scrollLeft + event.deltaX;
          props.onWheelScroll(scrollPos);
          if (isScrollingWithinScrollbarBounds(scrollPos, maxScrollPos)) {
            event.preventDefault();
          }
        }
      },
      onResize: () => {
        if (ref.current && context.viewport && computedStyle) {
          onSizesChange({
            content: context.viewport.scrollWidth,
            viewport: context.viewport.offsetWidth,
            scrollbar: {
              size: ref.current.clientWidth,
              paddingStart: toInt(computedStyle.paddingLeft),
              paddingEnd: toInt(computedStyle.paddingRight)
            }
          });
        }
      }
    }
  );
});
var ScrollAreaScrollbarY = reactExports.forwardRef((props, forwardedRef) => {
  const { sizes, onSizesChange, ...scrollbarProps } = props;
  const context = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
  const [computedStyle, setComputedStyle] = reactExports.useState();
  const ref = reactExports.useRef(null);
  const composeRefs = useComposedRefs(forwardedRef, ref, context.onScrollbarYChange);
  reactExports.useEffect(() => {
    if (ref.current) setComputedStyle(getComputedStyle(ref.current));
  }, [ref]);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    ScrollAreaScrollbarImpl,
    {
      "data-orientation": "vertical",
      ...scrollbarProps,
      ref: composeRefs,
      sizes,
      style: {
        top: 0,
        right: context.dir === "ltr" ? 0 : void 0,
        left: context.dir === "rtl" ? 0 : void 0,
        bottom: "var(--radix-scroll-area-corner-height)",
        ["--radix-scroll-area-thumb-height"]: getThumbSize(sizes) + "px",
        ...props.style
      },
      onThumbPointerDown: (pointerPos) => props.onThumbPointerDown(pointerPos.y),
      onDragScroll: (pointerPos) => props.onDragScroll(pointerPos.y),
      onWheelScroll: (event, maxScrollPos) => {
        if (context.viewport) {
          const scrollPos = context.viewport.scrollTop + event.deltaY;
          props.onWheelScroll(scrollPos);
          if (isScrollingWithinScrollbarBounds(scrollPos, maxScrollPos)) {
            event.preventDefault();
          }
        }
      },
      onResize: () => {
        if (ref.current && context.viewport && computedStyle) {
          onSizesChange({
            content: context.viewport.scrollHeight,
            viewport: context.viewport.offsetHeight,
            scrollbar: {
              size: ref.current.clientHeight,
              paddingStart: toInt(computedStyle.paddingTop),
              paddingEnd: toInt(computedStyle.paddingBottom)
            }
          });
        }
      }
    }
  );
});
var [ScrollbarProvider, useScrollbarContext] = createScrollAreaContext(SCROLLBAR_NAME);
var ScrollAreaScrollbarImpl = reactExports.forwardRef((props, forwardedRef) => {
  const {
    __scopeScrollArea,
    sizes,
    hasThumb,
    onThumbChange,
    onThumbPointerUp,
    onThumbPointerDown,
    onThumbPositionChange,
    onDragScroll,
    onWheelScroll,
    onResize,
    ...scrollbarProps
  } = props;
  const context = useScrollAreaContext(SCROLLBAR_NAME, __scopeScrollArea);
  const [scrollbar, setScrollbar] = reactExports.useState(null);
  const composeRefs = useComposedRefs(forwardedRef, (node) => setScrollbar(node));
  const rectRef = reactExports.useRef(null);
  const prevWebkitUserSelectRef = reactExports.useRef("");
  const viewport = context.viewport;
  const maxScrollPos = sizes.content - sizes.viewport;
  const handleWheelScroll = useCallbackRef(onWheelScroll);
  const handleThumbPositionChange = useCallbackRef(onThumbPositionChange);
  const handleResize = useDebounceCallback(onResize, 10);
  function handleDragScroll(event) {
    if (rectRef.current) {
      const x = event.clientX - rectRef.current.left;
      const y = event.clientY - rectRef.current.top;
      onDragScroll({ x, y });
    }
  }
  reactExports.useEffect(() => {
    const handleWheel = (event) => {
      const element = event.target;
      const isScrollbarWheel = scrollbar == null ? void 0 : scrollbar.contains(element);
      if (isScrollbarWheel) handleWheelScroll(event, maxScrollPos);
    };
    document.addEventListener("wheel", handleWheel, { passive: false });
    return () => document.removeEventListener("wheel", handleWheel, { passive: false });
  }, [viewport, scrollbar, maxScrollPos, handleWheelScroll]);
  reactExports.useEffect(handleThumbPositionChange, [sizes, handleThumbPositionChange]);
  useResizeObserver(scrollbar, handleResize);
  useResizeObserver(context.content, handleResize);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    ScrollbarProvider,
    {
      scope: __scopeScrollArea,
      scrollbar,
      hasThumb,
      onThumbChange: useCallbackRef(onThumbChange),
      onThumbPointerUp: useCallbackRef(onThumbPointerUp),
      onThumbPositionChange: handleThumbPositionChange,
      onThumbPointerDown: useCallbackRef(onThumbPointerDown),
      children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        Primitive.div,
        {
          ...scrollbarProps,
          ref: composeRefs,
          style: { position: "absolute", ...scrollbarProps.style },
          onPointerDown: composeEventHandlers(props.onPointerDown, (event) => {
            const mainPointer = 0;
            if (event.button === mainPointer) {
              const element = event.target;
              element.setPointerCapture(event.pointerId);
              rectRef.current = scrollbar.getBoundingClientRect();
              prevWebkitUserSelectRef.current = document.body.style.webkitUserSelect;
              document.body.style.webkitUserSelect = "none";
              if (context.viewport) context.viewport.style.scrollBehavior = "auto";
              handleDragScroll(event);
            }
          }),
          onPointerMove: composeEventHandlers(props.onPointerMove, handleDragScroll),
          onPointerUp: composeEventHandlers(props.onPointerUp, (event) => {
            const element = event.target;
            if (element.hasPointerCapture(event.pointerId)) {
              element.releasePointerCapture(event.pointerId);
            }
            document.body.style.webkitUserSelect = prevWebkitUserSelectRef.current;
            if (context.viewport) context.viewport.style.scrollBehavior = "";
            rectRef.current = null;
          })
        }
      )
    }
  );
});
var THUMB_NAME = "ScrollAreaThumb";
var ScrollAreaThumb = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { forceMount, ...thumbProps } = props;
    const scrollbarContext = useScrollbarContext(THUMB_NAME, props.__scopeScrollArea);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(Presence, { present: forceMount || scrollbarContext.hasThumb, children: /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollAreaThumbImpl, { ref: forwardedRef, ...thumbProps }) });
  }
);
var ScrollAreaThumbImpl = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { __scopeScrollArea, style, ...thumbProps } = props;
    const scrollAreaContext = useScrollAreaContext(THUMB_NAME, __scopeScrollArea);
    const scrollbarContext = useScrollbarContext(THUMB_NAME, __scopeScrollArea);
    const { onThumbPositionChange } = scrollbarContext;
    const composedRef = useComposedRefs(
      forwardedRef,
      (node) => scrollbarContext.onThumbChange(node)
    );
    const removeUnlinkedScrollListenerRef = reactExports.useRef(void 0);
    const debounceScrollEnd = useDebounceCallback(() => {
      if (removeUnlinkedScrollListenerRef.current) {
        removeUnlinkedScrollListenerRef.current();
        removeUnlinkedScrollListenerRef.current = void 0;
      }
    }, 100);
    reactExports.useEffect(() => {
      const viewport = scrollAreaContext.viewport;
      if (viewport) {
        const handleScroll = () => {
          debounceScrollEnd();
          if (!removeUnlinkedScrollListenerRef.current) {
            const listener = addUnlinkedScrollListener(viewport, onThumbPositionChange);
            removeUnlinkedScrollListenerRef.current = listener;
            onThumbPositionChange();
          }
        };
        onThumbPositionChange();
        viewport.addEventListener("scroll", handleScroll);
        return () => viewport.removeEventListener("scroll", handleScroll);
      }
    }, [scrollAreaContext.viewport, debounceScrollEnd, onThumbPositionChange]);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      Primitive.div,
      {
        "data-state": scrollbarContext.hasThumb ? "visible" : "hidden",
        ...thumbProps,
        ref: composedRef,
        style: {
          width: "var(--radix-scroll-area-thumb-width)",
          height: "var(--radix-scroll-area-thumb-height)",
          ...style
        },
        onPointerDownCapture: composeEventHandlers(props.onPointerDownCapture, (event) => {
          const thumb = event.target;
          const thumbRect = thumb.getBoundingClientRect();
          const x = event.clientX - thumbRect.left;
          const y = event.clientY - thumbRect.top;
          scrollbarContext.onThumbPointerDown({ x, y });
        }),
        onPointerUp: composeEventHandlers(props.onPointerUp, scrollbarContext.onThumbPointerUp)
      }
    );
  }
);
ScrollAreaThumb.displayName = THUMB_NAME;
var CORNER_NAME = "ScrollAreaCorner";
var ScrollAreaCorner = reactExports.forwardRef(
  (props, forwardedRef) => {
    const context = useScrollAreaContext(CORNER_NAME, props.__scopeScrollArea);
    const hasBothScrollbarsVisible = Boolean(context.scrollbarX && context.scrollbarY);
    const hasCorner = context.type !== "scroll" && hasBothScrollbarsVisible;
    return hasCorner ? /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollAreaCornerImpl, { ...props, ref: forwardedRef }) : null;
  }
);
ScrollAreaCorner.displayName = CORNER_NAME;
var ScrollAreaCornerImpl = reactExports.forwardRef((props, forwardedRef) => {
  const { __scopeScrollArea, ...cornerProps } = props;
  const context = useScrollAreaContext(CORNER_NAME, __scopeScrollArea);
  const [width, setWidth] = reactExports.useState(0);
  const [height, setHeight] = reactExports.useState(0);
  const hasSize = Boolean(width && height);
  useResizeObserver(context.scrollbarX, () => {
    var _a;
    const height2 = ((_a = context.scrollbarX) == null ? void 0 : _a.offsetHeight) || 0;
    context.onCornerHeightChange(height2);
    setHeight(height2);
  });
  useResizeObserver(context.scrollbarY, () => {
    var _a;
    const width2 = ((_a = context.scrollbarY) == null ? void 0 : _a.offsetWidth) || 0;
    context.onCornerWidthChange(width2);
    setWidth(width2);
  });
  return hasSize ? /* @__PURE__ */ jsxRuntimeExports.jsx(
    Primitive.div,
    {
      ...cornerProps,
      ref: forwardedRef,
      style: {
        width,
        height,
        position: "absolute",
        right: context.dir === "ltr" ? 0 : void 0,
        left: context.dir === "rtl" ? 0 : void 0,
        bottom: 0,
        ...props.style
      }
    }
  ) : null;
});
function toInt(value) {
  return value ? parseInt(value, 10) : 0;
}
function getThumbRatio(viewportSize, contentSize) {
  const ratio = viewportSize / contentSize;
  return isNaN(ratio) ? 0 : ratio;
}
function getThumbSize(sizes) {
  const ratio = getThumbRatio(sizes.viewport, sizes.content);
  const scrollbarPadding = sizes.scrollbar.paddingStart + sizes.scrollbar.paddingEnd;
  const thumbSize = (sizes.scrollbar.size - scrollbarPadding) * ratio;
  return Math.max(thumbSize, 18);
}
function getScrollPositionFromPointer(pointerPos, pointerOffset, sizes, dir = "ltr") {
  const thumbSizePx = getThumbSize(sizes);
  const thumbCenter = thumbSizePx / 2;
  const offset = pointerOffset || thumbCenter;
  const thumbOffsetFromEnd = thumbSizePx - offset;
  const minPointerPos = sizes.scrollbar.paddingStart + offset;
  const maxPointerPos = sizes.scrollbar.size - sizes.scrollbar.paddingEnd - thumbOffsetFromEnd;
  const maxScrollPos = sizes.content - sizes.viewport;
  const scrollRange = dir === "ltr" ? [0, maxScrollPos] : [maxScrollPos * -1, 0];
  const interpolate = linearScale([minPointerPos, maxPointerPos], scrollRange);
  return interpolate(pointerPos);
}
function getThumbOffsetFromScroll(scrollPos, sizes, dir = "ltr") {
  const thumbSizePx = getThumbSize(sizes);
  const scrollbarPadding = sizes.scrollbar.paddingStart + sizes.scrollbar.paddingEnd;
  const scrollbar = sizes.scrollbar.size - scrollbarPadding;
  const maxScrollPos = sizes.content - sizes.viewport;
  const maxThumbPos = scrollbar - thumbSizePx;
  const scrollClampRange = dir === "ltr" ? [0, maxScrollPos] : [maxScrollPos * -1, 0];
  const scrollWithoutMomentum = clamp(scrollPos, scrollClampRange);
  const interpolate = linearScale([0, maxScrollPos], [0, maxThumbPos]);
  return interpolate(scrollWithoutMomentum);
}
function linearScale(input, output) {
  return (value) => {
    if (input[0] === input[1] || output[0] === output[1]) return output[0];
    const ratio = (output[1] - output[0]) / (input[1] - input[0]);
    return output[0] + ratio * (value - input[0]);
  };
}
function isScrollingWithinScrollbarBounds(scrollPos, maxScrollPos) {
  return scrollPos > 0 && scrollPos < maxScrollPos;
}
var addUnlinkedScrollListener = (node, handler = () => {
}) => {
  let prevPosition = { left: node.scrollLeft, top: node.scrollTop };
  let rAF = 0;
  (function loop() {
    const position = { left: node.scrollLeft, top: node.scrollTop };
    const isHorizontalScroll = prevPosition.left !== position.left;
    const isVerticalScroll = prevPosition.top !== position.top;
    if (isHorizontalScroll || isVerticalScroll) handler();
    prevPosition = position;
    rAF = window.requestAnimationFrame(loop);
  })();
  return () => window.cancelAnimationFrame(rAF);
};
function useDebounceCallback(callback, delay) {
  const handleCallback = useCallbackRef(callback);
  const debounceTimerRef = reactExports.useRef(0);
  reactExports.useEffect(() => () => window.clearTimeout(debounceTimerRef.current), []);
  return reactExports.useCallback(() => {
    window.clearTimeout(debounceTimerRef.current);
    debounceTimerRef.current = window.setTimeout(handleCallback, delay);
  }, [handleCallback, delay]);
}
function useResizeObserver(element, onResize) {
  const handleResize = useCallbackRef(onResize);
  useLayoutEffect2(() => {
    let rAF = 0;
    if (element) {
      const resizeObserver = new ResizeObserver(() => {
        cancelAnimationFrame(rAF);
        rAF = window.requestAnimationFrame(handleResize);
      });
      resizeObserver.observe(element);
      return () => {
        window.cancelAnimationFrame(rAF);
        resizeObserver.unobserve(element);
      };
    }
  }, [element, handleResize]);
}
var Root = ScrollArea$1;
var Viewport = ScrollAreaViewport;
var Corner = ScrollAreaCorner;
function ScrollArea({
  className,
  children,
  ...props
}) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    Root,
    {
      "data-slot": "scroll-area",
      className: cn("relative", className),
      ...props,
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          Viewport,
          {
            "data-slot": "scroll-area-viewport",
            className: "focus-visible:ring-ring/50 size-full rounded-[inherit] transition-[color,box-shadow] outline-none focus-visible:ring-[3px] focus-visible:outline-1",
            children
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollBar, {}),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Corner, {})
      ]
    }
  );
}
function ScrollBar({
  className,
  orientation = "vertical",
  ...props
}) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    ScrollAreaScrollbar,
    {
      "data-slot": "scroll-area-scrollbar",
      orientation,
      className: cn(
        "flex touch-none p-px transition-colors select-none",
        orientation === "vertical" && "h-full w-2.5 border-l border-l-transparent",
        orientation === "horizontal" && "h-2.5 flex-col border-t border-t-transparent",
        className
      ),
      ...props,
      children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        ScrollAreaThumb,
        {
          "data-slot": "scroll-area-thumb",
          className: "bg-border relative flex-1 rounded-full"
        }
      )
    }
  );
}
const WATCH_HISTORY_KEY = "streamverse_watch_history";
function saveToLocalHistory(video) {
  try {
    const existing = JSON.parse(
      localStorage.getItem(WATCH_HISTORY_KEY) ?? "[]"
    );
    const filtered = existing.filter((e) => e.videoId !== video.videoId);
    localStorage.setItem(
      WATCH_HISTORY_KEY,
      JSON.stringify(
        [
          {
            videoId: video.videoId,
            title: video.title,
            thumbnailUrl: video.thumbnailUrl,
            watchedAt: Date.now(),
            platform: video.platform
          },
          ...filtered
        ].slice(0, 50)
      )
    );
  } catch {
  }
}
function getLocalWatchHistory() {
  try {
    return JSON.parse(
      localStorage.getItem(WATCH_HISTORY_KEY) ?? "[]"
    );
  } catch {
    return [];
  }
}
function clearLocalWatchHistory() {
  localStorage.removeItem(WATCH_HISTORY_KEY);
}
function getEmbedUrl(video) {
  if (video.platform === "vimeo") {
    return `https://player.vimeo.com/video/${video.videoId}?autoplay=1&title=0&byline=0&portrait=0`;
  }
  if (video.platform === "tiktok") {
    return `https://www.tiktok.com/embed/v2/${video.videoId}`;
  }
  return `https://www.youtube.com/embed/${video.videoId}?autoplay=1&rel=0&modestbranding=1`;
}
function getExternalUrl(video) {
  if (video.platform === "vimeo") return `https://vimeo.com/${video.videoId}`;
  if (video.platform === "tiktok")
    return `https://www.tiktok.com/@user/video/${video.videoId}`;
  return `https://www.youtube.com/watch?v=${video.videoId}`;
}
function timeAgo(ts, isRTL) {
  const diffMs = Date.now() - Number(ts);
  const diffMin = Math.floor(diffMs / 6e4);
  if (diffMin < 1) return isRTL ? "الآن" : "now";
  if (diffMin < 60) return isRTL ? `منذ ${diffMin} دقيقة` : `${diffMin}m ago`;
  const diffH = Math.floor(diffMin / 60);
  if (diffH < 24) return isRTL ? `منذ ${diffH} ساعة` : `${diffH}h ago`;
  const diffD = Math.floor(diffH / 24);
  return isRTL ? `منذ ${diffD} يوم` : `${diffD}d ago`;
}
function getInitials(id) {
  return id.toString().slice(0, 2).toUpperCase();
}
function CommentItem({
  comment,
  currentUserId,
  videoId,
  index
}) {
  const { isRTL } = useTranslation();
  const { actor } = useActor();
  const queryClient = useQueryClient();
  const [editing, setEditing] = reactExports.useState(false);
  const [editText, setEditText] = reactExports.useState(comment.text);
  const isOwn = currentUserId === comment.authorId.toString();
  const editMutation = useMutation({
    mutationFn: async (text) => {
      if (!actor) return;
      await actor.editComment(comment.id, text);
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ["comments", videoId] });
      setEditing(false);
    }
  });
  const deleteMutation = useMutation({
    mutationFn: async () => {
      if (!actor) return;
      await actor.deleteComment(comment.id);
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ["comments", videoId] });
    }
  });
  if (comment.isDeleted) return null;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "comment-card", "data-ocid": `comment.item.${index + 1}`, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex-shrink-0 w-8 h-8 rounded-full gradient-primary flex items-center justify-center text-xs font-bold text-white", children: getInitials(comment.authorId.toString()) }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex-1 min-w-0 space-y-1", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 justify-between", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-xs font-semibold text-foreground truncate", children: [
          comment.authorId.toString().slice(0, 8),
          "…"
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-[10px] text-muted-foreground flex-shrink-0", children: [
          timeAgo(comment.createdAt, isRTL),
          comment.isEdited && /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "ms-1 opacity-60", children: [
            "(",
            isRTL ? "معدّل" : "edited",
            ")"
          ] })
        ] })
      ] }),
      editing ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-2", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          Textarea,
          {
            value: editText,
            onChange: (e) => setEditText(e.target.value),
            className: "text-sm min-h-[60px] bg-background border-border resize-none",
            "data-ocid": `comment.edit_textarea.${index + 1}`
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex gap-1.5", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Button,
            {
              size: "sm",
              className: "h-7 text-xs gradient-primary text-white border-0",
              onClick: () => editMutation.mutate(editText),
              disabled: editMutation.isPending || !editText.trim(),
              "data-ocid": `comment.save_button.${index + 1}`,
              children: editMutation.isPending ? /* @__PURE__ */ jsxRuntimeExports.jsx(LoaderCircle, { className: "h-3 w-3 animate-spin" }) : isRTL ? "حفظ" : "Save"
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Button,
            {
              size: "sm",
              variant: "ghost",
              className: "h-7 text-xs",
              onClick: () => setEditing(false),
              "data-ocid": `comment.cancel_button.${index + 1}`,
              children: isRTL ? "إلغاء" : "Cancel"
            }
          )
        ] })
      ] }) : /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm text-foreground/90 leading-relaxed", children: comment.text }),
      isOwn && !editing && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex gap-1", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "button",
          {
            type: "button",
            className: "text-[10px] text-muted-foreground hover:text-foreground transition-colors flex items-center gap-0.5",
            onClick: () => setEditing(true),
            "data-ocid": `comment.edit_button.${index + 1}`,
            "aria-label": isRTL ? "تعديل" : "Edit",
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Pencil, { className: "h-2.5 w-2.5" }),
              isRTL ? "تعديل" : "Edit"
            ]
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-muted-foreground/30 mx-1", children: "·" }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "button",
          {
            type: "button",
            className: "text-[10px] text-destructive/60 hover:text-destructive transition-colors flex items-center gap-0.5",
            onClick: () => deleteMutation.mutate(),
            disabled: deleteMutation.isPending,
            "data-ocid": `comment.delete_button.${index + 1}`,
            "aria-label": isRTL ? "حذف" : "Delete",
            children: [
              deleteMutation.isPending ? /* @__PURE__ */ jsxRuntimeExports.jsx(LoaderCircle, { className: "h-2.5 w-2.5 animate-spin" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Trash2, { className: "h-2.5 w-2.5" }),
              isRTL ? "حذف" : "Delete"
            ]
          }
        )
      ] })
    ] })
  ] });
}
function CommentsPanel({ video, currentUserId }) {
  const { isRTL } = useTranslation();
  const { actor } = useActor();
  const queryClient = useQueryClient();
  const [newComment, setNewComment] = reactExports.useState("");
  const textareaRef = reactExports.useRef(null);
  const { data: comments = [], isLoading } = useQuery({
    queryKey: ["comments", video.videoId],
    queryFn: async () => {
      if (!actor) return [];
      return actor.getVideoComments(video.videoId);
    },
    enabled: !!actor,
    staleTime: 1e3 * 30
  });
  const addMutation = useMutation({
    mutationFn: async (text) => {
      if (!actor) return;
      await actor.addComment(video.videoId, null, text);
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ["comments", video.videoId] });
      setNewComment("");
    }
  });
  const visibleComments = comments.filter((c) => !c.isDeleted);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      className: "comment-thread",
      "data-ocid": "comments.panel",
      dir: isRTL ? "rtl" : "ltr",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 mb-1", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(MessageCircle, { className: "h-4 w-4 text-primary" }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("h3", { className: "font-display font-semibold text-sm text-foreground", children: [
            isRTL ? "التعليقات" : "Comments",
            visibleComments.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "ms-1.5 text-xs text-muted-foreground font-normal", children: [
              "(",
              visibleComments.length,
              ")"
            ] })
          ] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex gap-2 items-start", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex-shrink-0 w-7 h-7 rounded-full gradient-accent flex items-center justify-center text-[10px] font-bold text-white mt-1", children: currentUserId ? getInitials(currentUserId) : "?" }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex-1 space-y-2", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              Textarea,
              {
                ref: textareaRef,
                value: newComment,
                onChange: (e) => setNewComment(e.target.value),
                placeholder: isRTL ? "أضف تعليقاً..." : "Add a comment...",
                className: "text-sm min-h-[70px] bg-background border-border resize-none text-foreground placeholder:text-muted-foreground",
                "data-ocid": "comments.new_comment_textarea",
                onKeyDown: (e) => {
                  if (e.key === "Enter" && (e.ctrlKey || e.metaKey) && newComment.trim()) {
                    addMutation.mutate(newComment.trim());
                  }
                }
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsxs(
              Button,
              {
                size: "sm",
                onClick: () => addMutation.mutate(newComment.trim()),
                disabled: addMutation.isPending || !newComment.trim(),
                className: "h-8 text-xs gradient-primary text-white border-0 gap-1.5",
                "data-ocid": "comments.submit_button",
                children: [
                  addMutation.isPending ? /* @__PURE__ */ jsxRuntimeExports.jsx(LoaderCircle, { className: "h-3.5 w-3.5 animate-spin" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Send, { className: "h-3.5 w-3.5" }),
                  isRTL ? "نشر" : "Post"
                ]
              }
            )
          ] })
        ] }),
        isLoading ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "space-y-3 mt-2", "data-ocid": "comments.loading_state", children: [1, 2, 3].map((i) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex gap-3", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Skeleton, { className: "w-8 h-8 rounded-full flex-shrink-0" }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex-1 space-y-1.5", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Skeleton, { className: "h-3 w-24" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Skeleton, { className: "h-4 w-full" })
          ] })
        ] }, i)) }) : visibleComments.length === 0 ? /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "div",
          {
            className: "text-center py-6 text-muted-foreground",
            "data-ocid": "comments.empty_state",
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(MessageCircle, { className: "h-8 w-8 mx-auto mb-2 opacity-30" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm", children: isRTL ? "لا توجد تعليقات بعد" : "No comments yet" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs mt-0.5", children: isRTL ? "كن أول من يعلق!" : "Be the first to comment!" })
            ]
          }
        ) : /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollArea, { className: "max-h-72", children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "space-y-2 pe-2", children: visibleComments.map((c, i) => /* @__PURE__ */ jsxRuntimeExports.jsx(
          CommentItem,
          {
            comment: c,
            currentUserId,
            videoId: video.videoId,
            index: i
          },
          c.id
        )) }) })
      ]
    }
  );
}
function VideoPlayer({
  video,
  videoUrl,
  onClose,
  onShare
}) {
  var _a, _b;
  const { t, isRTL } = useTranslation();
  const { actor } = useActor();
  const overlayRef = reactExports.useRef(null);
  const [showComments, setShowComments] = reactExports.useState(false);
  const [downloading, setDownloading] = reactExports.useState(false);
  const [subtitles, setSubtitles] = reactExports.useState([]);
  const [activeSubtitle, setActiveSubtitle] = reactExports.useState(null);
  const subtitleInputRef = reactExports.useRef(null);
  function srtToVtt(srt) {
    const converted = srt.replace(/\r\n/g, "\n").replace(/\r/g, "\n").replace(/\d+\n(\d{2}:\d{2}:\d{2}),(\d{3})/g, "$1.$2").replace(
      /(\d{2}:\d{2}:\d{2}),(\d{3}) --> (\d{2}:\d{2}:\d{2}),(\d{3})/g,
      "$1.$2 --> $3.$4"
    );
    return `WEBVTT

${converted}`;
  }
  async function handleSubtitleUpload(e) {
    var _a2;
    const file = (_a2 = e.target.files) == null ? void 0 : _a2[0];
    if (!file) return;
    const text = await file.text();
    let vttContent = text;
    if (file.name.endsWith(".srt")) {
      vttContent = srtToVtt(text);
    }
    const blob = new Blob([vttContent], { type: "text/vtt" });
    const url = URL.createObjectURL(blob);
    const label = file.name.replace(/\.(srt|vtt)$/i, "");
    setSubtitles((prev) => [...prev, { url, label, language: "custom" }]);
    setActiveSubtitle(url);
    ue.success(
      isRTL ? `✓ تم إضافة الترجمة: ${label}` : `✓ Subtitle added: ${label}`
    );
  }
  const [optimisticReaction, setOptimisticReaction] = reactExports.useState(null);
  const queryClient = useQueryClient();
  const { data: userProfile } = useQuery({
    queryKey: ["my-profile"],
    queryFn: async () => {
      if (!actor) return null;
      return actor.getCallerUserProfile();
    },
    enabled: !!actor
  });
  const currentUserId = ((_a = userProfile == null ? void 0 : userProfile.id) == null ? void 0 : _a.toString()) ?? null;
  const { data: stats } = useQuery({
    queryKey: ["video-stats", video == null ? void 0 : video.videoId],
    queryFn: async () => {
      if (!actor || !video) return null;
      return actor.getVideoStats(video.videoId);
    },
    enabled: !!actor && !!video,
    staleTime: 1e3 * 30
  });
  const { data: myReaction } = useQuery({
    queryKey: ["video-reaction", video == null ? void 0 : video.videoId],
    queryFn: async () => {
      if (!actor || !video) return null;
      return actor.getVideoReaction(video.videoId);
    },
    enabled: !!actor && !!video
  });
  const reactionMutation = useMutation({
    mutationFn: async (kind) => {
      if (!actor || !video) return;
      await actor.reactToVideo(video.videoId, kind);
    },
    onSuccess: () => {
      if (!video) return;
      queryClient.invalidateQueries({
        queryKey: ["video-stats", video.videoId]
      });
      queryClient.invalidateQueries({
        queryKey: ["video-reaction", video.videoId]
      });
    }
  });
  reactExports.useEffect(() => {
    if (video) {
      saveToLocalHistory(video);
      document.body.style.overflow = "hidden";
    }
    return () => {
      document.body.style.overflow = "";
    };
  }, [video]);
  reactExports.useEffect(() => {
    const handleKey = (e) => {
      if (e.key === "Escape") onClose();
    };
    document.addEventListener("keydown", handleKey);
    return () => document.removeEventListener("keydown", handleKey);
  }, [onClose]);
  if (!video) return null;
  const platformLabel = video.platform === "vimeo" ? /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "inline-flex items-center gap-1 text-primary font-semibold text-xs", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "w-2 h-2 rounded-full bg-primary" }),
    "Vimeo"
  ] }) : video.platform === "tiktok" ? /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "span",
    {
      className: "inline-flex items-center gap-1 font-semibold text-xs",
      style: { color: "#fe2c55" },
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "span",
          {
            className: "w-2 h-2 rounded-full",
            style: { background: "#fe2c55" }
          }
        ),
        "TikTok"
      ]
    }
  ) : /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "inline-flex items-center gap-1 text-red-400 font-semibold text-xs", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "w-2 h-2 rounded-full bg-red-500" }),
    "YouTube"
  ] });
  const likeCount = stats ? Number(stats.likeCount) : 0;
  const dislikeCount = stats ? Number(stats.dislikeCount) : 0;
  const commentCount = stats ? Number(stats.commentCount) : 0;
  const activeReaction = myReaction ?? optimisticReaction;
  const handleDownload = async () => {
    if (!video || downloading) return;
    setDownloading(true);
    try {
      await handleVideoDownload(
        video.platform,
        video.videoId,
        videoUrl,
        video.title,
        t
      );
    } finally {
      setDownloading(false);
    }
  };
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "div",
    {
      ref: overlayRef,
      "data-ocid": "video_player.dialog",
      className: "fixed inset-0 z-50 flex flex-col items-center justify-start md:justify-center bg-black/95 backdrop-blur-sm animate-fade-in overflow-y-auto py-4",
      onClick: (e) => {
        if (e.target === overlayRef.current) onClose();
      },
      onKeyDown: (e) => {
        if (e.key === "Escape") onClose();
      },
      tabIndex: -1,
      "aria-modal": "true",
      "aria-label": `Playing: ${video.title}`,
      dir: isRTL ? "rtl" : "ltr",
      children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: cn("relative w-full max-w-4xl mx-4 animate-slide-up"), children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-start justify-between gap-4 mb-3 px-1", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex-1 min-w-0", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { className: "font-display font-bold text-white text-base md:text-lg line-clamp-2", children: video.title }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-3 mt-0.5", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm text-white/60", children: video.channelTitle }),
              platformLabel
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-1 flex-shrink-0", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              Button,
              {
                variant: "ghost",
                size: "icon",
                type: "button",
                "data-ocid": "video_player.download_button",
                className: "text-white/70 hover:text-cyan-400 hover:bg-cyan-500/10 rounded-full transition-colors",
                onClick: handleDownload,
                disabled: downloading,
                "aria-label": isRTL ? "تحميل الفيديو" : "Download video",
                title: isRTL ? "تحميل الفيديو" : "Download video",
                children: downloading ? /* @__PURE__ */ jsxRuntimeExports.jsx(LoaderCircle, { className: "h-4 w-4 animate-spin" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Download, { className: "h-4 w-4" })
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              "a",
              {
                href: getExternalUrl(video),
                target: "_blank",
                rel: "noopener noreferrer",
                "data-ocid": "video_player.external_link",
                className: "inline-flex items-center justify-center w-9 h-9 text-white/70 hover:text-white hover:bg-white/10 rounded-full transition-colors",
                "aria-label": video.platform === "vimeo" ? "Open on Vimeo" : "Open on YouTube",
                children: /* @__PURE__ */ jsxRuntimeExports.jsx(ExternalLink, { className: "h-4 w-4" })
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              Button,
              {
                variant: "ghost",
                size: "icon",
                type: "button",
                "data-ocid": "video_player.share_button",
                className: "text-white/70 hover:text-white hover:bg-white/10 rounded-full",
                onClick: onShare,
                "aria-label": t("share"),
                children: /* @__PURE__ */ jsxRuntimeExports.jsx(Share2, { className: "h-4 w-4" })
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              Button,
              {
                variant: "ghost",
                size: "icon",
                type: "button",
                "data-ocid": "video_player.close_button",
                className: "text-white/70 hover:text-white hover:bg-white/10 rounded-full",
                onClick: onClose,
                "aria-label": t("close"),
                children: /* @__PURE__ */ jsxRuntimeExports.jsx(X, { className: "h-5 w-5" })
              }
            )
          ] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "div",
          {
            className: "relative rounded-xl overflow-hidden bg-black shadow-2xl",
            style: { paddingBottom: "56.25%" },
            children: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                "iframe",
                {
                  src: getEmbedUrl(video) ?? "",
                  title: video.title,
                  allow: "accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; fullscreen",
                  allowFullScreen: true,
                  className: "absolute inset-0 w-full h-full"
                }
              ),
              activeSubtitle && subtitles.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "absolute bottom-10 left-0 right-0 flex justify-center pointer-events-none z-10 px-4", children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "bg-black/80 text-white text-sm px-3 py-1.5 rounded-lg text-center max-w-sm backdrop-blur-sm", children: (_b = subtitles.find((s) => s.url === activeSubtitle)) == null ? void 0 : _b.label }) })
            ] })
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "mt-2 px-1 flex items-center gap-2 flex-wrap", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "input",
            {
              ref: subtitleInputRef,
              type: "file",
              accept: ".vtt,.srt",
              className: "hidden",
              onChange: (e) => void handleSubtitleUpload(e),
              "aria-label": isRTL ? "رفع ملف ترجمة" : "Upload subtitle file"
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "button",
            {
              type: "button",
              onClick: () => {
                var _a2;
                return (_a2 = subtitleInputRef.current) == null ? void 0 : _a2.click();
              },
              "data-ocid": "video_player.upload_subtitle_button",
              className: "flex items-center gap-1.5 text-xs text-white/60 hover:text-white transition-colors px-2 py-1 rounded-lg hover:bg-white/10",
              "aria-label": isRTL ? "إضافة ترجمة" : "Add subtitle",
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Upload, { className: "h-3.5 w-3.5" }),
                isRTL ? "ترجمة" : "Subtitles"
              ]
            }
          ),
          subtitles.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-1 flex-wrap", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              "button",
              {
                type: "button",
                onClick: () => setActiveSubtitle(null),
                "data-ocid": "video_player.subtitle_off_button",
                className: `text-xs px-2 py-1 rounded-lg transition-colors ${!activeSubtitle ? "bg-white/20 text-white" : "text-white/50 hover:text-white hover:bg-white/10"}`,
                children: isRTL ? "بدون ترجمة" : "Off"
              }
            ),
            subtitles.map((sub) => /* @__PURE__ */ jsxRuntimeExports.jsxs(
              "button",
              {
                type: "button",
                onClick: () => setActiveSubtitle(sub.url),
                "data-ocid": "video_player.subtitle_toggle",
                className: `text-xs px-2 py-1 rounded-lg transition-colors flex items-center gap-1 ${activeSubtitle === sub.url ? "bg-primary/30 text-primary" : "text-white/50 hover:text-white hover:bg-white/10"}`,
                children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx(Captions, { className: "h-3 w-3" }),
                  sub.label
                ]
              },
              sub.url
            ))
          ] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "mt-3 px-1 flex items-center justify-between gap-2 bg-white/5 rounded-xl p-3", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            SocialBar,
            {
              videoId: video.videoId,
              videoUrl,
              platform: video.platform,
              likeCount,
              dislikeCount,
              commentCount,
              userReaction: activeReaction,
              onLike: () => {
                setOptimisticReaction("like");
                reactionMutation.mutate("like");
              },
              onDislike: () => {
                setOptimisticReaction("dislike");
                reactionMutation.mutate("dislike");
              },
              onComment: () => setShowComments((v) => !v),
              onShare
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "button",
            {
              type: "button",
              "data-ocid": "video_player.toggle_comments_button",
              onClick: () => setShowComments((v) => !v),
              className: "flex items-center gap-1.5 text-xs text-white/60 hover:text-white transition-colors ms-auto",
              "aria-label": isRTL ? "التعليقات" : "Toggle comments",
              children: showComments ? /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(ChevronUp, { className: "h-4 w-4" }),
                isRTL ? "إخفاء التعليقات" : "Hide comments"
              ] }) : /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(ChevronDown, { className: "h-4 w-4" }),
                isRTL ? "عرض التعليقات" : "View comments"
              ] })
            }
          )
        ] }),
        showComments && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "mt-3", children: /* @__PURE__ */ jsxRuntimeExports.jsx(CommentsPanel, { video, currentUserId }) })
      ] })
    }
  );
}
export {
  DownloadModal as D,
  ScrollArea as S,
  VideoPlayer as V,
  SocialBar as a,
  Share2 as b,
  clearLocalWatchHistory as c,
  getLocalWatchHistory as g
};
